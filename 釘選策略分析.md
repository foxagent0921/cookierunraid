# 釘選策略分析：釘藍+紫 vs 釘藍+白

打怪賽雕刻召喚石只能釘選 2 格、其餘 3 格必須重骰。本文比較兩種釘法的期望成本，資料來源為 `遊戲參數整理.txt`（14 張遊戲截圖整理）。

**結論：方案 b 勝出，快 2.0～12.4 倍，而且是數學上必然，不是特例。**

> ⚠️ **2026-08-04 已依修正後的群組權重模型重算**（修正內容見 [釘選策略評估驗證紀錄.md](釘選策略評估驗證紀錄.md) 第二節）。舊版數字（每格 0.277%～1.109%、快 3.3～12.9 倍）已作廢。**結論本身不變**：方案 b 在結構上就是方案 a 的超集，且修正後的每格命中率上限 1.865% 仍遠低於 3.78% 的交叉點。

> **本文的目標是兩個「指定」的白字數值格**（每格約 0.29%～1.87%）。若目標改成「任意」推薦能力（白字每格約 16.1%、藍字約 20.2%），結論會反過來——見 [推薦能力洗法分析.md](推薦能力洗法分析.md)。兩者不矛盾，純粹是目標機率相差約 14 倍導致最佳解翻轉。
>
> 中間還有一種常見情況：**指定群組，但該群組內任一 ★ 推薦格都收**。這會讓大群組（例如群組16，24 個白字項目裡有 9 個推薦）的命中率放大 9 倍，足以翻轉「該釘哪一格」的答案——見第六節。

> 想直接算自己的目標，開 `index.html` 切到「**釘選策略評估**」分頁，選 2 個白字目標即可得到單轉成功率、期望轉數與落袋政策建議。本文提供的是背後的推導。

---

## 一、問題設定

301 階以上召喚石的 5 個附加能力欄位固定為：

| 欄位 | 來源 | 說明 |
| --- | --- | --- |
| 1 格紫字（高級） | 群組19 | 9 項各 11.11%，301階以上必定出現 |
| 1 格藍字（中級） | 群組1-18 的 44 個 `*` 項目 | 31階以上必定出現，選中 1 個後其他藍字全排除；其中 9 個標 `**` 的是想要的推薦藍字 |
| 3 格白字（低級） | 群組1-18 的其餘項目 | 群組互不重複；其中 27 個標 `#` 的是想要的推薦白字 |

**目前狀態**：紫字已經是想要的，藍字也已經是 9 個 `**` 推薦藍字之一，還差 **2 個指定的白字項目**（指定到單一數值格，例如「暴擊傷害增加70-80%」那一格；實務上會從 27 個 `#` 推薦白字裡挑）。

**兩種釘法**：

- **方案 a** — 釘住藍字 + 紫字，重骰 3 個白字，一次湊齊 2 個目標。
- **方案 b** — 釘住藍字，放掉紫字；先在白字裡拼到 1 個目標並釘住，之後重骰的紫字 + 2 個白字要同時命中。

---

## 二、模型與假設

假設全部列在這裡，方便日後檢驗或修正。

1. **紫字 = 群組19，命中想要的那個 = 1/9。**
   群組19 的 9 個項目各 11.11%，且該群組全部是高級能力（`script.js` 的 `HIGH_GRADE_GROUP_ID = 19`）。301 階以上必定出現 1 個高級能力，因此這一格恆為群組19，只是項目隨機。

2. **藍字在兩個方案中都全程釘住，不影響比較。**
   兩方案的差異只在紫字與白字，藍字為共同項，可從比較中消去。

3. **白字每格命中率 `a = 白字格抽中該群組的機率 × 群組內低級項目機率`。**
   兩個因子都**不是**直接從「參數速查」抄的原始值：
   - 抽取順序是「群組 → 項目」，而白字格保證是低級能力，所以群組機率要先乘上「該群組吐出低級的比例」再於各群組間重新正規化。例如群組1 的原始群組機率 4.57%，白字格實際抽中它的機率是 4.6303%。
   - 群組內項目機率的分母只有該群組的低級項目（群組1 為 96.96%），不是整個群組。

   舊版本文直接用「原始群組機率 × 原始項目機率」，那是未正規化的近似；差異最大的是群組3（1.109% → 1.865%）。網站「釘選策略評估」分頁的欄位提示會直接顯示這兩個因子與相乘結果，可直接對照。

   ⚠️ 第二個因子取的是**指定單一數值格**的機率。若你的實際標準是「這個群組裡任一 ★ 推薦格都收」，分子要換成推薦項目的**機率合計**，`a` 會整個放大——大群組放大得特別多，足以翻轉結論。見第六節。

4. **3 個白字格近似為獨立抽取。**
   實際上群組不可重複會造成微小負相關（同一群組不會連中兩次），方向是讓真實成功率**略高於**本文估計。兩方案同向受影響，不改變結論。

5. **資料限制：群組3 的 4 項為推論補齊**。截圖只揭露到 62.12%（5 項白字 12.12% + 1 項藍字 1.52%），缺的部分依群組10 的同型結構補上「餅乾所受傷害量減少」11-20%／21-30%／31-40%（各 12.12%，白字）與 41-50%（1.52%，藍字），補齊後合計恰為 100.00%，且群組3 與群組10 的計算結果完全一致。這 4 項不是截圖實測值，若實際組成不同，群組3 的 `a` 值與其餘群組的藍字權重都會跟著變。其餘 18 個群組已逐一核對，合計皆 ≈100%，仍不自行推測補齊。

---

## 三、公式推導

令兩個目標白字的每格命中率為 `a`、`b`。

### 方案 a

單轉要在 3 格白字裡同時湊到兩個目標。用排容原理：

```
P_a = 1 − (1−a)³ − (1−b)³ + (1−a−b)³
```

`a`、`b` 都很小時展開得 `P_a ≈ 6ab`（3 格挑 1 格放 A、剩 2 格挑 1 格放 B）。

期望轉數 `N_a = 1 / P_a`。

### 方案 b

**階段 1**：釘住藍字 + 紫字，重骰 3 格白字，直到落袋 1 個目標。
注意這個動作**和方案 a 完全一樣**——差別只在方案 b 中一個目標就願意收手。

**階段 2**：改釘藍字 + 已落袋的白字，重骰「紫字 + 2 格白字」。單轉成功需要紫字中想要的（1/9）**且**剩下那個目標出現在 2 格白字之一：

```
P₂(c) = (1/9) × [1 − (1−c)²]        c = 剩下那個目標的命中率
```

階段 1 單轉有四種結果：兩個都中（直接完工）、只中難抽的、只中好抽的、都沒中。設難抽的為 `hard = min(a,b)`、好抽的為 `easy = max(a,b)`，令

```
E_hard = 1 / P₂(hard)     已落袋好抽的，之後要拼難抽的
E_easy = 1 / P₂(easy)     已落袋難抽的，之後要拼好抽的
```

兩種落袋政策的閉式解：

```
先到先釘   V = (1 + p_難只中 · E_easy + p_易只中 · E_hard) / (1 − p_都沒中)
只釘難的   V = (1 + p_難只中 · E_easy) / (1 − p_易只中 − p_都沒中)
```

方案 b 的期望轉數取兩者較小值。

### 交叉點

令 `a = b`，用近似式比較：

```
N_a ≈ 1/(6a²)
N_b ≈ 1/(6a) + 9/(2a) = 28/(6a)
```

相等時 `1/a = 28`，即 `a ≈ 3.57%`。用完整公式數值求解得 **`a ≈ 3.78%`**。交叉點只由這組公式決定，與群組權重模型無關，所以 2026-08-04 的修正沒有改變它。

**但單一指定白字的每格命中率上限只有 1.865%**（群組3，見附錄 A），距交叉點還差 2.0 倍；扣掉資料不完整的群組3 後，實際常見值約 1.16%，差 3.3 倍——**永遠到不了**。

---

## 四、數值比較

| 目標對（每格命中率） | 方案a 單轉成功率 | 方案a 期望轉數 | b：先到先釘 | b：只釘難的 | b 最佳 | b 快幾倍 |
| --- | --- | --- | --- | --- | --- | --- |
| 1.865% + 1.865%（皆群組3） | 0.2048% | 488 | **248** | 253 | 248 | 2.0x |
| 1.158% + 1.158%（常見值） | 0.0795% | 1,257 | **401** | 411 | 401 | 3.1x |
| 1.158% + 0.579%（＋群組16） | 0.0399% | 2,508 | 665 | **440** | 440 | 5.7x |
| 0.579% + 0.579%（皆群組16） | 0.0200% | 5,001 | **804** | 828 | 804 | 6.2x |
| 1.158% + 0.290%（＋群組5技能） | 0.0200% | 4,999 | 1,340 | **497** | 497 | 10.1x |
| 0.290% + 0.290%（皆群組5技能） | 0.0050% | 19,875 | **1,607** | 1,660 | 1,607 | 12.4x |

粗體為該情境下較佳的落袋政策。可以看到規律：兩目標機率**相同**時「先到先釘」較佳，**差距大**時「只釘難的」較佳。

---

## 五、結論與實務建議

### 1. 選方案 b，而且不必猶豫

方案 b 的階段 1 就是「釘藍+紫、重骰 3 白」，與方案 a 是**完全同一個動作**；差別只在方案 b 多了一個「中 1 個就落袋、改打第二階段」的出場選項。

**方案 b 是方案 a 的超集**，期望轉數不可能比方案 a 差——這是結構上的保證，不依賴任何數值。實測快 2.0～12.4 倍。

直覺上的理由：方案 a 要求「3 格白字裡同時中 2 個約 1% 的目標」，這是兩個小機率相乘（約萬分之八）；方案 b 把它拆成兩個獨立關卡，先用便宜的代價鎖住一半戰果，之後只需要對付「1/9 × 一個 1% 目標」。**放掉已經到手的紫字看似浪費，但 1/9 遠比再湊一個 1% 的白字便宜。**

### 2. 落袋政策：看兩個目標的機率差距

- **兩目標命中率接近** → **先到先釘**（中哪個釘哪個）。硬等特定的那個只是浪費轉數。
- **兩目標命中率差距大** → **只釘難抽的那個**，把好抽的留到階段 2。
  以 1.158% + 0.579% 為例：只釘難的 440 轉 vs 先到先釘 665 轉，差了 1.5 倍。
  道理是階段 2 的成本由「剩下那個目標」決定，所以要把好抽的留在後面。

### 3. 階段 1 若一轉同時中 2 個，直接收工

這正是方案 a 的成功事件，機率雖低（約 0.005%～0.2%）但發生了就是賺到，不需要再進階段 2。

### 4. 目標愈難，方案 b 的優勢愈大

從表中可見倍數隨命中率下降而放大（2.0x → 12.4x）。若兩個目標都落在群組5 這種每格僅 0.290% 的技能項目，方案 a 要近兩萬轉，實務上等同不可行。

---

## 六、目標範圍：指定單一格 vs 群組任一推薦

前五節的每一個數字都預設「目標＝指定到單一數值格」。實務上很多人的標準其實是「這個群組裡任一 ★ 推薦格都收」，而這兩者差多少，**取決於群組有多大**：

| 群組 | 白字項目數 | 其中 ★ 推薦 | 指定 1 格 | 任一推薦 | 放大倍數 |
| --- | --- | --- | --- | --- | --- |
| 群組1 | 4 | 2 | 25.00% | 50.00% | 2.0x |
| 群組16 | 24 | 9 | 4.17% | 37.50% | **9.0x** |

群組16 的群組機率高（14.14%）但項目多達 24 個，所以「指定一格」很難、「任一推薦」很好拼。**只看群組機率會把難易判斷反過來。**

### 釘選格與重骰格的不對稱

這是最容易忽略的一點：**釘選格不吃這個設定**。

釘選代表那一格已經在手上，是一個既定的單一能力，計算上恆為 100%——把它放寬成「任一推薦」不會讓它變成別的能力，也不會改變任何數字。**變好抽的只有本輪還要重骰的格子。**

於是同一組目標，換一種釘法，兩格的難易可能整個對調。以「藍字釘群組5、白字目標是群組1 與群組16 各一格、重骰紫字＋2 白字」為例（每轉 5,000 點）：

| 群組1 / 群組16 的目標範圍 | 釘群組1、拼群組16 | 釘群組16、拼群組1 | 勝出 |
| --- | --- | --- | --- |
| 指定 / 指定 | 655.7 轉 | **271.6 轉** | 釘群組16（2.4x） |
| 指定 / 任一推薦 | **72.9 轉** | 271.6 轉 | 釘群組1（3.7x） |
| 任一推薦 / 指定 | 655.7 轉 | **135.8 轉** | 釘群組16（4.8x） |
| 任一推薦 / 任一推薦 | **72.9 轉** | 135.8 轉 | 釘群組1（1.9x） |

同樣兩個群組，光是改變「我有多挑」，該釘哪一格就換了兩次答案。

### 翻盤點

群組16 每格命中率 ＝ 13.905% × (k/24)，k 是可接受的格數：

- **k = 2**（1.159%）就已經追平「群組1 指定 1 格」的 1.158%
- **k = 4**（2.318%）追平「群組1 任一推薦 2 格」的 2.315%
- k = 9（5.215%）遠遠超過

只要對群組16 的接受度超過 1 格，第四節那張表的方向就不適用了。

### 原則沒變，只是要寫完整

```
每格命中率 = 白字池群組占比 × (群組內所有可接受項目的機率合計 ÷ 群組內低級項目合計)
```

分子是**可接受項目的合計**，不是「指定一項」。策略本身仍然是「**釘難的、拼容易的**」——只是難易要用這條完整的式子算，不能只看群組機率，也不能假設別人的目標範圍跟你一樣。

網站「釘選策略評估」分頁的每個藍字、白字欄位都有「指定單一格 / 任一推薦」切換鈕，切換後會直接顯示放大倍數與重算後的期望轉數；**兩種釘法都各按一次再決定**。

---

## 附錄 A：白字每格命中率速查

單一指定白字項目的 `a = 白字格抽中該群組 × 群組內低級項目機率`（修正後模型）：

| 群組 | 原始群組機率 | 白字格抽中此群組 | 群組內低級項目機率 | 每格命中率 `a` | 2026-08-04 修正前 |
| --- | --- | --- | --- | --- | --- |
| 3 | 9.15% | 9.2751% | 12.50% | **1.159%**（上限） | 1.109% |
| 10 | 9.15% | 9.2751% | 12.50% | **1.159%**（上限） | 1.109% |
| 6 | 13.72% | 13.9090% | 8.33% | 1.159% | 1.109% |
| 1、2、4、7、8、9、14、15 | 4.57% | 4.6330% | 25.00% | 1.158% | 1.108% |
| 16 | 14.14% | 13.9132% | 4.17% | 0.580% | 0.554% |
| 5（出現技能能量果凍） | 14.13% | 14.3879% | 3.33% | 0.479% | 0.458% |
| 13 | 2.22% | 2.1759% | 20.00% | 0.435% | 0.416% |
| 5（各色技能項目） | 14.13% | 14.3879% | 2.01% | **0.290%**（下限） | 0.277% |
| 11、12、17、18 | — | — | — | 無白字項目（全為中級） | — |

**實際範圍 0.290% ～ 1.159%，全部遠低於 3.78% 的交叉點。**

註：多數群組修正後只上升約 4%（低級占比接近 100%，各群組等比縮放後相互抵銷）。**群組3 在 2026-08-14 補齊資料後不再是離群值**：補齊前它的項目合計僅 62.12%，可見項目中有 97.55% 是低級，`a` 被算成 1.865%；補上「餅乾所受傷害量減少」的 4 項後，低級占比回到 96.96%，`a` 降為 1.159%，與同為 9.15%、同型結構的群組10 完全一致。上限因此從 1.865% 下修為 1.159%，結論（全部遠低於交叉點）不變。

---

## 附錄 B：驗算

以下為純 node 腳本（無外部套件），可重算本文所有數字。存成檔案後執行 `node 檔名.js`：

```js
"use strict";
const miss3 = (p) => Math.pow(1 - p, 3);
const miss2 = (p) => Math.pow(1 - p, 2);

// 階段2：釘住藍字 + 已落袋的白字，重骰「紫字 + 2 個白字」
// 單轉成功 = 紫字中想要的(1/9) 且 2 格白字至少命中剩下那個目標
const stage2 = (r) => (1 / 9) * (1 - miss2(r));

function evaluate(a, b) {
  const hard = Math.min(a, b);
  const easy = Math.max(a, b);

  // 方案 a：釘藍+紫，重骰 3 白，單轉要同時湊到兩個目標
  const pBoth = 1 - miss3(a) - miss3(b) + miss3(a + b);

  // 階段1 單轉的四種結果
  const pHardOnly = 1 - miss3(hard) - pBoth;
  const pEasyOnly = 1 - miss3(easy) - pBoth;
  const pNone = 1 - pBoth - pHardOnly - pEasyOnly;

  const eHard = 1 / stage2(hard); // 已落袋易抽的，還要拼難抽的
  const eEasy = 1 / stage2(easy); // 已落袋難抽的，還要拼易抽的

  return {
    pBoth,
    planA: 1 / pBoth,
    bankFirst: (1 + pHardOnly * eEasy + pEasyOnly * eHard) / (1 - pNone),
    bankHard: (1 + pHardOnly * eEasy) / (1 - pEasyOnly - pNone),
  };
}

// 每格命中率取自附錄A（修正後模型）
const CASES = [
  ["1.865% + 1.865%", 0.01865, 0.01865],
  ["1.158% + 1.158%", 0.01158, 0.01158],
  ["1.158% + 0.579%", 0.01158, 0.00579],
  ["0.579% + 0.579%", 0.00579, 0.00579],
  ["1.158% + 0.290%", 0.01158, 0.00290],
  ["0.290% + 0.290%", 0.00290, 0.00290],
];

const round = (n) => Math.round(n).toLocaleString("en-US");
console.log("目標對(每格命中率)  | 方案a單轉 | 方案a期望 | b先到先釘 | b只釘難的 | b最佳 | 倍數");
for (const [label, a, b] of CASES) {
  const r = evaluate(a, b);
  const best = Math.min(r.bankFirst, r.bankHard);
  console.log(
    label.padEnd(19), "|",
    (r.pBoth * 100).toFixed(4) + "%", "|",
    round(r.planA).padStart(9), "|",
    round(r.bankFirst).padStart(9), "|",
    round(r.bankHard).padStart(9), "|",
    round(best).padStart(5), "|",
    (r.planA / best).toFixed(1) + "x",
  );
}

// 交叉點：兩目標命中率相同時，a 要多高方案 a 才會勝出
let lo = 0.0001;
let hi = 0.5;
for (let i = 0; i < 200; i += 1) {
  const mid = (lo + hi) / 2;
  const r = evaluate(mid, mid);
  if (r.planA > Math.min(r.bankFirst, r.bankHard)) lo = mid; else hi = mid;
}
console.log("\n交叉點 a=b =", (lo * 100).toFixed(2) + "%（單一指定白字的實際上限僅 1.865%）");
```
