# 推薦能力洗法分析：釘紫+藍 vs 釘藍+白 vs 釘白+白

打怪賽雕刻召喚石只能釘選 2 格、其餘 3 格必須重骰。本文比較三種釘法在「**任意**推薦能力」這個目標下的期望花費（點數），資料來源為 `遊戲參數整理.txt`（14 張遊戲截圖整理），機率標記依 `script.js` 的 `RECOMMENDED_ITEMS_BY_GRADE`。

**結論：方案 a 大幅勝出，且可再優化 8%。**

> ⚠️ **2026-08-04（第二次）：推薦白字從 23 項增為 24 項後全面重算**。新增的是群組10 的「體力恢復量增加11-15%」。**三條路線的排名與所有質性結論都不變**，但所有數值都變便宜了（白字每格 13.72% → 14.93%，路線 a 175,425 → 144,681）。受影響最大的是〔七〕群組16 的優化幅度：從省 11.3% 縮到省 8.2%——因為群組10 現在提供了第二個非群組16 的白字來源，「避開群組16」的相對價值下降。
>
> ⚠️ **2026-08-04（第一次）：已依修正後的群組權重模型全面重算**（修正內容見 [釘選策略評估驗證紀錄.md](釘選策略評估驗證紀錄.md) 第二節）。修正的重點：抽群組時，群組機率必須先乘上「該群組真的吐出該等級的比例」再正規化。
> 本文舊版〔二〕列出的「法A：綜合權重（群組機率×項目機率）直接歸一化」其實接近正確做法，卻被判定為「粗略近似」而採用了法B——**當時的蒙地卡羅之所以支持法B，是因為模擬本身也用了同一個錯誤的群組抽法**，所以驗不出問題。

> 本文與 [釘選策略分析.md](釘選策略分析.md) 比較的是**同一組路線代號 a/b/c，但目標定義不同、結論相反**——那份文件的目標是兩個**指定**的白字數值格（每格約 0.29%～1.87%），本文的目標是「任意」推薦能力（白字每格約 14.9%、藍字約 20.1%）。兩者並不矛盾，差異純粹來自目標機率相差約 13 倍，見〔五、與《釘選策略分析》的關係〕。

---

## 一、問題設定

301 階以上召喚石的 5 個附加能力欄位固定為 1 紫 + 1 藍 + 3 白（欄位定義同 [釘選策略分析.md](釘選策略分析.md) 一節）。

**目標**（對應原始提問的三種洗法）：

- **紫字**：必出 1 個（群組19），中目標項機率 = **1/9 ≈ 11.11%**
- **藍字**：必出 1 個，目標 = 9 個標記 `**` 的推薦藍字之一
- **白字**：3 格中要湊到 **2 個**標記 `#` 的推薦白字（24 項之一）

**釘選費用**：洗 0 個釘選 2000 點／轉，釘 1 個 3000 點／轉，釘 2 個 5000 點／轉。

**三種路線**（對應原提問 a/b/c）：

- **a）** 釘紫字＋藍字，重骰 3 白，拼出 2 個推薦白字
- **b）** 釘藍字，先在白字裡拼到 1 個推薦白字並釘住，再重骰「紫字＋剩下 1 個白字」
- **c）** 先在白字裡拼到 2 個推薦白字都釘住，再重骰「紫字＋藍字」

---

## 二、三個關鍵機率

### 紫字：1/9 ≈ 11.11%

群組19（`HIGH_GRADE_GROUP_ID`）固定 9 個項目、各 11.11%，題目已直接給定。

### 藍字：≈ 20.1%

藍字池為群組1–18 的 43 個 `*` 項目，其中 9 個標 `**`：

| 算法 | 藍字命中推薦(`**`)機率 |
| --- | --- |
| ❌ 舊模型：依原始群組機率抽群組，再於群組內藍字項目歸一化 | 18.45% |
| ✅ **修正後：群組權重＝群組機率 × 該群組中級占比** | **20.13%** |
| 參考：綜合權重（群組機率×項目機率）直接歸一化、不含群組互斥 | 20.51% |

舊模型的問題是把「**含有**中級項目的群組」一視同仁，但藍字格保證是中級，而群組11/12/17/18 的項目全是中級（抽中必定是中級）、群組5 只有 2.60% 是中級。權重必須按這個比例縮放。修正後的 20.13% 與「綜合權重歸一化」的 20.51% 接近，差額來自模擬有納入群組不可重複的互斥效應。

9 個 `**` 藍字分布：群組1（1項）、群組5（4項）、群組16（4項）——**只集中在這三個群組**，這件事後面會反覆用到。

### 白字：每格 ≈ 14.9%

白字池為群組1–18 的低級項目，其中 24 個標 `#`（2026-08-04 新增群組10 的「體力恢復量增加11-15%」）：

| 算法 | 白字每格命中推薦(`#`)機率 |
| --- | --- |
| 單格封閉式（僅排除群組19） | 15.35% |
| ✅ **模擬（含群組互斥，本文採用）** | **14.93%** |
| 參考：23 項時的同口徑數值 | 13.72% |

新增一項讓白字每格上升 1.21 個百分點（13.72% → 14.93%）。群組10 的白字權重是 2.32pt，因為該群組每個項目都是 12.12%、且整組只有 1 項中級，等於單一項目就佔了白字火力的 7.5%；標記兩項就翻倍成 15.1%。

24 個 `#` 白字分布：群組1（2）、群組5（9）、群組9（1）、群組10（**2**）、群組15（2）、群組16（8）。以火力（權重×群組內占比）計，群組16 仍是最大宗但已從 32.7% 降到 **30.2%**，群組10 則從 8.2% 升到 15.1%——**推薦白字不再那麼集中在群組1/5/16**（三者合計從 67.4% 降到 62.3%），這正是後段群組16 優化幅度縮水的原因。

---

## 三、主要結果（全保真模擬，150,000 次／策略）

模擬完整還原「群組不可重複」的兩階段抽取與逐轉計費（方法見附錄C），非公式近似。

| 路線 | 期望總點數 | 標準誤 | 中位數 | 90%分位 | 倍數 |
| --- | --- | --- | --- | --- | --- |
| **a）釘紫+藍，最後拼白** | 144,681 | ±317 | 109,000 | 303,000 | 1.09x |
| b）釘藍→加釘白，最後拼紫+白 | 223,313 | ±539 | 160,000 | 496,000 | 1.68x |
| c）先釘2白，最後拼紫+藍 | 483,808 | ±1,706 | 263,000 | 1,151,000 | 3.65x |
| **a★）a 的優化版（見七）** | **132,617** | ±280 | 102,000 | 274,000 | **1.00x** |

分布右偏明顯（平均 144,681 但中位數只要 109,000）——**抓預算建議看 90% 分位（303,000），不要只看平均值**，否則容易低估需要準備的點數。

新增第 24 個 `#` 後三條路線全面變便宜，但**受益程度不同**：a 省 17.5%、b 省 10.9%、c 只省 6.2%。a 受益最多是因為它的終局完全靠白字（3 選 2），白字變好抽直接打在最痛的地方；c 的終局卡在紫字與藍字，白字變好只縮短了前置的「湊 2 白」階段，所以 c 與 a 的差距反而從 2.9 倍拉大到 **3.3 倍**。

### 為什麼 a 大幅領先

三條路線終局都是 5000 點／轉，勝負全看終局單轉成功率：

| 終局狀態 | 要同時中什麼 | 單轉成功率 |
| --- | --- | --- |
| 釘紫+藍（a） | 3 白中 2 個 `#` | **4.14%** |
| 釘紫+白 | `**`藍 + 2白中≥1個`#` | 3.49% |
| 釘藍+白（b） | 紫(1/9) + 2白中≥1個`#` | 2.47% |
| 釘白+白（c） | 紫(1/9) + `**`藍 | 1.48% |

原則：**該釘住的是最難的兩格**。紫字（11.1%）與藍字（20.1%）都是單格、無備援；白字雖然每格只有 14.9%，但 3 格輪流抽、只要 2 中，自帶備援。a 把兩個最難、無備援的鎖住，讓最便宜、有備援的白字做「3 選 2」；c 反過來，把兩個最難的留到最後一起賭，等於把 1/9 × 20.1% 硬壓在同一轉上。

只有 c 的終局不含白字，所以新增第 24 個 `#` 後只有它幾乎沒動（1.30% → 1.48%，且那點漲幅來自落點分布變好而非終局本身）；其餘三個終局都直接受惠。「釘紫+藍」與「釘紫+白」的差距重新拉開到 0.65 個百分點（23 項時只差 0.26pt），先鎖白字的相對代價又變高了。

---

## 四、追加發現一：紫字這一格，值得釘

**單一釘位隔離測試**（各自單獨釘住，30,000 次）：

| 策略 | 期望點數 | 期望轉數 |
| --- | --- | --- |
| 完全不釘（2000/轉） | 2,131,752 ±12,326 | 1,066 轉 |
| **只釘紫（3000/轉）** | **372,518 ±2,062** | **124 轉** |
| 只釘藍（3000/轉） | 663,757 ±3,782 | 221 轉 |
| 只釘白（3000/轉，1格） | 886,794 ±6,246 | 296 轉 |

紫字 11.1% 看似三者最低，但它是唯一「單格無備援」中最難的，所以省下的期望轉數最多——釘紫讓單價漲 50%（2000→3000），卻讓期望轉數砍掉 88%。

**直接對照「允許 vs 禁止釘紫」**（各 60,000 次）：

| | 期望點數 |
| --- | --- |
| 禁止釘紫的最佳解（只能藍+白） | 229,993 ±868 |
| 允許釘紫的最佳解（紫+藍） | 145,037 ±497 |

那一個釘位值 **85,000 點（省 37%）**。白字變好抽之後，這個差距比 23 項時（省 30%）更大：白字愈容易湊齊，被紫字卡住的比重就愈高，紫字那一格的相對價值也就愈突出。**「紫字優先」的結論在新資料下更穩固。**

**小結**：優先序 **紫字 ＞ 藍字 ＞ 白字**。沒中紫字時建議整盤空洗（2000/轉），把釘位留給紫字，不要為了保留藍/白而多付釘選費——這個調整能再省約 1%（在雜訊範圍邊緣，順手做就好，不必勉強）。

---

## 五、追加發現二：白字群組互斥（縮池）效應

**問題**：白字選走特定群組後，藍字的候選池變少、機率上升的可能有沒有算進去？

**答案**：有算進去（模擬引擎本來就是重新歸一化的生成過程），而且效果**方向不對稱**——18 個群組裡，封鎖任一個對藍字命中 `**` 的影響：

| 封鎖的群組 | 藍字命中`**` | 變化 |
| --- | --- | --- |
| 群組6 | 22.35% | **+2.30pt ↑**（最大漲幅） |
| 群組10 / 17 | 21.66% / 21.58% | +1.60 / +1.53pt ↑ |
| 群組3 | 21.29% | +1.23pt ↑ |
| 群組2,4,7,8,9,11,12,13,14,15 | 20.74～20.89% | +0.69～0.83pt ↑ |
| 群組18 | 20.54% | +0.48pt ↑ |
| 群組5 | 19.02% | −1.04pt ↓ |
| 群組1 | 17.30% | −2.76pt ↓ |
| **群組16** | **7.89%** | **−12.17pt ↓**（最大跌幅） |

（基準 20.06%）18 個群組裡 **15 個是上升的**，只有 1、5、16 下降——因為 `**` 只住在這三個群組。修正後的排序與舊版幾乎相同，只有群組3 從「+0.75pt 那一群」上升到 +1.23pt（它的中級占比只有 2.45%，封鎖它釋出的權重比舊模型算的更多）。

**但路線 c 剛好最容易踩到下降的那 3 個**：`#` 白字火力有 62.3% 來自群組1/5/16。把「釘住的2個 `#` 白字」實際落點依權重算出來：

- 藍字機率**上升的機率只有 18.9%**，下降 81.1%
- 期望 `**` 機率從 20.08% 掉到 **13.31%**

而且「成本 = 1/機率」不是線性的，壞配置的懲罰被放大（Jensen 效應）：

```
期望終局成本 E[1/p] = 490,861 點
若用「期望機率 13.31%」反推 1/E[p] 只有 338,149 點
```

差的 15.3 萬點，就是壞配置在期望值裡被放大的權重。最常落到的四種組合仍然都含群組16：

| 釘住的白字群組 | 出現機率 | 終局單轉成功率 |
| --- | --- | --- |
| 群組15 + 16 | 11.2% | 0.919% |
| **群組1 + 16** | 11.1% | **0.413%**（最糟） |
| 群組5 + 16 | 10.2% | 0.547% |
| 群組10 + 16 | 10.0% | 0.963% |
| 群組1 + 15 | 7.1% | 1.985% |
| 群組5 + 15 | 6.7% | 2.195% |

最糟的「群組1+16」單轉只剩 0.413%，卻有 11.1% 的機率遇到。**這才是路線 c 真正貴的原因**，比單看平均機率更嚴重。新增群組10 的第二個 `#` 之後，落點分布被稀釋得平均一些（最糟組合的出現率從 14.4% 降到 11.1%，且多出「群組10+16」這個相對溫和的分支），所以 c 也跟著便宜了一點——但它受益幅度仍是三條路線裡最小的。

**反方向也檢查了**：釘住 `**` 藍字後，白字池只會下降、不會上升——因為 `**` 只在群組1/5/16，而這三個群組同時是白字大戶：

| 藍字來自 | 白字`#`每格 | 變化 |
| --- | --- | --- |
| 群組5 | 14.83% | −0.10pt |
| 群組1 | 13.49% | −1.45pt |
| 群組16 | 12.57% | −2.36pt |

（基準 14.93%）沒有上升分支，因為藍/白的推薦目標在群組16 是完全互斥的（見七）。三個分支的跌幅都比 23 項時小，其中群組5 幾乎已經無感（−0.30pt → −0.10pt）。

**抽取順序的敏感度**（藍字先抽 vs 白字先抽，各 40,000 次）：

| 路線 | 藍先抽 | 白先抽 | 差異 |
| --- | --- | --- | --- |
| a | 145,702 | 145,770 | +0.0%（雜訊內） |
| b | 223,759 | 222,825 | −0.4%（雜訊內） |
| c | 485,784 | 535,990 | **+10.3%** |

只有 c 對順序敏感（±620～1,040 的標準誤下，a／b 的差異都落在雜訊內）——因為群組16 的藍字權重高，「白字先抽走群組16」對藍字的傷害最大。本文採用的順序（藍先抽）對 c 較有利，所以排名結論不受此假設影響：即使用對 c 最有利的假設，它仍是三者中最貴的。

---

## 六、追加發現三：紫字有 1/9，值得釘嗎？

**是的，而且是三格裡最值得的一格。** 已在〔四〕用「禁止釘紫」的顯式對照證明（省 37%）。這裡補充直覺：1/9 看似偏高，但它比另外兩個對手更慘的地方在於「單格無備援」——藍字同樣單格無備援但機率更高（20.1%），白字雖然每格才 14.9% 卻有 3 格輪流抽、只要 2 中的容錯。三者放在一起比較，紫字才是最該優先鎖住的。白字新增第 24 個推薦項後更好湊，瓶頸進一步集中到紫字，這個結論只會更成立。

---

## 七、追加發現四：藍字該不該接受群組16？

群組16 的推薦項目彼此互斥（同一顆石頭不可能同時出現）：

| 稀有度 | 內容（4條技能線 × 對應數值） |
| --- | --- |
| `**`藍字（4項） | 果凍魔法師／閃避大師 餅乾傷害量增加**50%**、2個以上時增加**50%** |
| `#`白字（8項） | 同 4 條技能線的 **30% / 40%** 版本 |

釘住群組16 的 `**` 藍字，會把 8 個 `#` 白字（30.2% 的白字火力）一起排除，白字每格從 14.93% 掉到 **12.57%**。

**120,000 次／策略、附標準誤的對照**：

| 策略 | 期望總點數 | 中位數 |
| --- | --- | --- |
| 有紫才釘，藍白皆不挑群組（基準） | 144,335 ±351 | 110,000 |
| 只有白字避開群組16 | 142,275 ±356 | 106,000 |
| **藍字避開群組16** | **138,031 ±320** | 108,000 |
| 藍、白都避開群組16 | 132,485 ±314 | 101,000 |
| 上一項 + 沒白可釘時仍接受群組16藍 | 132,823 ±315 | 102,000 |

相對基準的節省（95% 信賴區間）：

| 策略 | 節省 | 是否顯著 |
| --- | --- | --- |
| 只有白字避開群組16 | 2,060 ±979（1.4%） | ✓ 顯著（但幅度小） |
| 藍字避開群組16 | 6,305 ±930（4.4%） | ✓ 顯著 |
| 藍、白都避開群組16 | 11,850 ±923（**8.2%**） | ✓ 顯著 |
| 上一項 + 沒白可釘時仍接受16藍 | 11,513 ±924（8.0%） | ✓ 顯著，但與上一列無法區分 |

**這條優化在新增第 24 個 `#` 之後縮水了**（11.3% → 8.2%），但仍然值得做。原因是群組10 現在提供第二個非群組16 的白字來源：被群組16 排擠掉的那 8 個 `#` 只佔 30.2% 的火力（原本 32.7%），而剩下能替代的火力變多了，所以「避開群組16」省下的相對比例下降。

**主要效益仍來自「藍字避開群組16」**（單獨就有 4.4%）。兩者合起來 8.2% 仍大於各自相加（1.4% + 4.4%），交互作用的機制不變：只有當你開始拒絕釘群組16 的藍字，你才會頻繁面臨「釘哪個白字」的抉擇，這時避開群組16 的白字才有意義。

**與舊版不同的一點**：最後一列「沒白可釘時仍接受群組16藍」在 23 項時被記為再省 0.5%，重跑後它比「藍、白都避開」貴 338 點（±924），**兩者差異落在雜訊內、無法區分**。實務上兩種做法可視為等價，不必為了這條規則增加操作負擔。

**重要但書**：這條優化只適用於「任意 `**` 藍字都算贏」的目標。如果實際需求是**指定**要群組16 的某條特定藍字（例如「2個閃避大師以上時餅乾傷害量增加50%」），上述分析不適用——必須接受釘住群組16、白字只能從群組1/5/9/10/15 的 16 個 `#` 裡湊。

---

## 八、最終建議

1. **走路線 a**：釘紫+藍，重骰白字拼 2 個推薦白字。比 b 快 1.5 倍、比 c 快 3.3 倍。
2. **沒中紫字時整盤空洗**（2000點/轉），不要為了保留藍/白多付釘選費。
3. **藍字若剛好是群組16 的 `**`，先別急著釘**——留給第二釘位去挑白字（省 8.2%）。等白字湊到 1 個推薦項後，若已經沒有非群組16 的白字可釘，接受或不接受群組16 的藍字都可以，兩者差異在雜訊內。
4. **預算抓 90% 分位（約 274,000～303,000 點）**，不要只用平均值規劃，因為花費分布右偏、中位數遠低於平均值。

按 1–3 執行後，期望總花費約 **133,000 點**（中位數 102,000）。

---

## 附錄 A：`**`/`#` 推薦項清單

**推薦藍字 `**`（9項）：**

| 群組 | 項目 |
| --- | --- |
| 1 | 餅乾造成的傷害量增加41-50% |
| 5 | 黃色/紅色/藍色技能傷害量增加41-50%、出現更多技能能量果凍 |
| 16 | 果凍魔法師餅乾傷害量增加50%、閃避大師餅乾傷害量增加50%、2個果凍魔法師以上時餅乾傷害量增加50%、2個閃避大師以上時餅乾傷害量增加50% |

**推薦白字 `#`（24項，依群組）：** 群組1（2項）、群組5（9項）、群組9（1項）、群組10（2項：體力恢復量增加11-15%／16-20%）、群組15（2項）、群組16（8項，見七）。完整清單見 `遊戲參數整理.txt` 中標 `#` 的項目，或 `script.js` 的 `RECOMMENDED_ITEMS_BY_GRADE`。

## 附錄 B：資料限制

群組3 的項目機率合計僅 62.12%（截圖未完整揭露），但該群組沒有任何 `#`／`**` 標記項目（新增第 24 個 `#` 落在群組10，不改變這一點），只影響白字池歸一化時的「雜訊比例」。修正後模型以可見項目重新正規化，等於假設沒有未揭露項目——這使群組3 的低級占比算成 97.55%（而非除以 100 得到的 60.60%），白字格抽中群組3 的機率因此略為上升，稀釋掉一點推薦命中率。由於群組3 完全沒有推薦項，它只是「雜訊」，不影響三路線的排名。沿用專案原則，不自行推測補齊未顯示的資料。

## 附錄 C：模擬方法與驗證腳本

**方法**：全保真 Monte Carlo，每一轉都用「先抽群組（不可重複）、再於群組內依項目機率抽項目」的兩階段流程產生 1 紫 + 1 藍 + 3 白，逐轉依已釘選格數計費（2000/3000/5000），直到終局條件（紫中目標 且 藍中`**` 且 白字≥2個`#`）達成為止，取多次模擬的平均總花費。

**2026-08-04（第二次）重跑**：本文所有模擬數字已在推薦白字增為 24 項後用同一支腳本重跑；重跑前先以 23 項的設定驗證腳本能重現前一版的數字（白字每格 13.73% 對 13.72%、路線 a 176,513 對 175,425、〔七〕節省 1.0%／7.3%／11.6% 對 1.0%／7.4%／11.3%），確認差異只來自資料本身而非實作。腳本本身未改動——`items` 的 `rec` 旗標直接讀文字檔的 `#`／`**` 標記，所以文字檔改標記後重跑即可。

**2026-08-04（第一次）修正**：抽群組的權重已改為「群組機率 × 該群組該等級占比」（見下方 `weight()`）。舊版直接用群組機率當權重，那正是被修正掉的錯誤模型；當時的蒙地卡羅之所以「驗證通過」，就是因為模擬與封閉式用了同一個錯誤假設。修正後的權重已用**拒絕取樣**（只照原始機率抽、等級不符就重抽，完全不用這條公式）獨立驗證，見 [釘選策略評估驗證紀錄.md](釘選策略評估驗證紀錄.md) 附錄 B。

以下為可重跑的 node 腳本骨架（需搭配 `遊戲參數整理.txt` 同目錄執行 `node 檔名.js`）：

```js
"use strict";
const fs = require("fs");
// 解析 遊戲參數整理.txt：取得每個群組的機率(groupRate)，
// 以及每個群組內項目的 {group, rate, grade(high/mid/low), rec(是否為 **/# 推薦項)}
const raw = fs.readFileSync("遊戲參數整理.txt", "utf8");
const groupRate = new Map();
for (const l of raw.split(/\r?\n/)) {
  const m = l.match(/^附加能力群組\s*(\d+)\s*\|\s*([\d.]+)%/);
  if (m) groupRate.set(+m[1], +m[2] / 100);
}
const items = []; let cur = null;
for (const l of raw.split(/\r?\n/)) {
  const g = l.match(/^\[附加能力群組\s*(\d+)\]/);
  if (g) { cur = +g[1]; continue; }
  if (cur === null) continue;
  const m = l.match(/^(\*\*|\*|#)?\s*(.+?)\s*\|\s*([\d.]+)%\s*$/);
  if (!m || /^附加能力群組/.test(m[2])) continue;
  const k = m[1] || "";
  items.push({
    group: cur, rate: +m[3] / 100,
    grade: cur === 19 ? "high" : (k === "*" || k === "**") ? "mid" : "low",
    rec: k === "**" || k === "#",
  });
}
const GROUPS = [...groupRate.keys()].filter((g) => g <= 18);
const midBy = new Map(GROUPS.map((g) => [g, items.filter((i) => i.group === g && i.grade === "mid")]));
const lowBy = new Map(GROUPS.map((g) => [g, items.filter((i) => i.group === g && i.grade === "low")]));

// 【2026-08-04 修正】群組權重＝群組機率 × (該群組該等級項目合計 ÷ 該群組全部項目合計)
// 舊版直接用 groupRate.get(x) 當權重，等於假設每個群組吐出該等級的機會相同，
// 會把群組11/12/17/18（項目全為中級）嚴重低估。分母用群組自身合計而非 100，
// 因為群組3 只揭露 62.12%。此權重已用拒絕取樣獨立驗證。
const total = (list) => list.reduce((s, i) => s + i.rate, 0);
const groupItemTotal = new Map(GROUPS.map((g) =>
  [g, total(items.filter((i) => i.group === g))]));
const gradeShare = (grade, g) =>
  total((grade === "mid" ? midBy : lowBy).get(g)) / groupItemTotal.get(g);
const weight = (grade, g) => groupRate.get(g) * gradeShare(grade, g);

function pick(list, weightOf) {
  let total = 0; for (const x of list) total += weightOf(x);
  let r = Math.random() * total;
  for (const x of list) { r -= weightOf(x); if (r <= 0) return x; }
  return list[list.length - 1];
}
// 兩階段抽取：先在未占用群組中依修正後權重抽群組，再於群組內依項目機率抽項目
function drawSlot(grade, used) {
  const by = grade === "mid" ? midBy : lowBy;
  const pool = GROUPS.filter((g) => !used.has(g) && by.get(g).length > 0);
  const g = pick(pool, (x) => weight(grade, x));
  used.add(g);
  return { group: g, rec: pick(by.get(g), (x) => x.rate).rec };
}

const COST = [2000, 3000, 5000];
// 路線 a：釘紫+藍，最後拼白
const policyA = (board) => ({ P: board.P, B: board.B.rec, W: [] });

function runOnce(policy) {
  let pin = { P: false, B: false, W: [] };
  let board = { P: false, B: { rec: false, group: null }, W: [null, null, null] };
  let cost = 0;
  for (let roll = 0; roll < 500000; roll += 1) {
    cost += COST[(pin.P ? 1 : 0) + (pin.B ? 1 : 0) + pin.W.length];
    const used = new Set([19]); // 紫字固定占用群組19
    if (pin.B) used.add(board.B.group);
    for (const i of pin.W) used.add(board.W[i].group);
    const next = { P: pin.P || Math.random() < 1 / 9, B: pin.B ? board.B : drawSlot("mid", used), W: [null, null, null] };
    for (let i = 0; i < 3; i += 1) next.W[i] = pin.W.includes(i) ? board.W[i] : null;
    for (let i = 0; i < 3; i += 1) if (!next.W[i]) next.W[i] = drawSlot("low", used);
    board = next;
    if (board.P && board.B.rec && board.W.filter((x) => x.rec).length >= 2) return cost;
    pin = policyA(board); // 換成其他路線的釘選函式即可比較
  }
  return cost;
}

const N = 150000;
const costs = Array.from({ length: N }, runOnce);
const mean = costs.reduce((a, b) => a + b, 0) / N;
console.log("期望總點數：", Math.round(mean).toLocaleString("en-US"));
```

三條路線與優化版的釘選函式（把 `policyA` 換成對應版本即可重算本文任何一個數字）：

```js
// b) 釘藍→加釘白，最後拼紫+白
const policyB = (b) => b.B.rec
  ? { P: false, B: true, W: b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i).slice(0, 1) }
  : { P: false, B: false, W: [] };

// c) 先釘2白，最後拼紫+藍
const policyC = (b) => ({ P: false, B: false, W: b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i).slice(0, 2) });

// a★) 優化版：沒中紫字不釘；藍字避開群組16；白字沒有非群組16的#時才退而求其次
const policyAStar = (b) => {
  if (!b.P) return { P: false, B: false, W: [] };
  if (b.B.rec && b.B.group !== 16) return { P: true, B: true, W: [] };
  const cands = b.W.map((x, i) => [x, i]).filter(([x]) => x.rec);
  const non16 = cands.filter(([x]) => x.group !== 16);
  return { P: true, B: false, W: (non16.length ? non16 : cands).slice(0, 1).map(([, i]) => i) };
};
```
