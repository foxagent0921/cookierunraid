# 推薦能力洗法分析：釘紫+藍 vs 釘藍+白 vs 釘白+白

打怪賽雕刻召喚石只能釘選 2 格、其餘 3 格必須重骰。本文比較三種釘法在「**任意**推薦能力」這個目標下的期望花費（點數），資料來源為 `遊戲參數整理.txt`（14 張遊戲截圖整理），機率標記依 `script.js` 的 `RECOMMENDED_ITEMS_BY_GRADE`。

**結論：方案 a 大幅勝出，且可再優化 9%。**

> 本文與 [釘選策略分析.md](釘選策略分析.md) 比較的是**同一組路線代號 a/b/c，但目標定義不同、結論相反**——那份文件的目標是兩個**指定**的白字數值格（每格僅約 1.1%），本文的目標是「任意」推薦能力（白字每格約 14.7%、藍字約 18–20%）。兩者並不矛盾，差異純粹來自目標機率相差約 13 倍，見〔五、與《釘選策略分析》的關係〕。

---

## 一、問題設定

301 階以上召喚石的 5 個附加能力欄位固定為 1 紫 + 1 藍 + 3 白（欄位定義同 [釘選策略分析.md](釘選策略分析.md) 一節）。

**目標**（對應原始提問的三種洗法）：

- **紫字**：必出 1 個（群組19），中目標項機率 = **1/9 ≈ 11.11%**
- **藍字**：必出 1 個，目標 = 9 個標記 `**` 的推薦藍字之一
- **白字**：3 格中要湊到 **2 個**標記 `#` 的推薦白字（23 項之一）

**釘選費用**：洗 0 個釘選 2000 點／轉，釘 1 個 3000 點／轉，釘 2 個 5000 點／轉。

**三種路線**（對應原提問 a/b/c）：

- **a）** 釘紫字＋藍字，重骰 3 白，拼出 2 個推薦白字
- **b）** 釘藍字，先在白字裡拼到 1 個推薦白字並釘住，再重骰「紫字＋剩下 1 個白字」
- **c）** 先在白字裡拼到 2 個推薦白字都釘住，再重骰「紫字＋藍字」

---

## 二、三個關鍵機率

### 紫字：1/9 ≈ 11.11%

群組19（`HIGH_GRADE_GROUP_ID`）固定 9 個項目、各 11.11%，題目已直接給定。

### 藍字：≈ 18.4%

藍字池為群組1–18 的 43 個 `*` 項目，其中 9 個標 `**`：

| 算法 | 藍字命中推薦(`**`)機率 |
| --- | --- |
| 法A：綜合權重（群組機率×項目機率）直接歸一化 | 20.51% |
| 法B：先依群組機率抽群組，再於群組內藍字項目歸一化 | 18.40% |
| 蒙地卡羅驗證（含「群組不可重複」+「恰好1個中級」拒絕抽樣，30萬次） | 18.44% |

遊戲實際抽取是「先抽群組、再抽群組內項目」的兩階段流程，法B 與蒙地卡羅一致，是正確值；法A 只是粗略近似。本文所有全保真模擬（見附錄C）都直接還原兩階段抽取，因此使用的有效機率 **≈ 18.4%**。

9 個 `**` 藍字分布：群組1（1項）、群組5（4項）、群組16（4項）——**只集中在這三個群組**，這件事後面會反覆用到。

### 白字：每格 ≈ 13.6%～14.7%

白字池為群組1–18 的低級項目，其中 23 個標 `#`：

| 算法 | 白字每格命中推薦(`#`)機率 |
| --- | --- |
| 綜合權重歸一化 | 14.71% |
| 未條件化的原始綜合機率 | 13.57% |
| 蒙地卡羅驗證（同上） | 13.81% |

實際區間落在 13.6%～14.7%，敏感度測試（附錄C腳本可重跑）顯示不影響三路線的排名。

23 個 `#` 白字分布：群組1（2）、群組5（9）、群組9（1）、群組10（1）、群組15（2）、群組16（8）。**群組16 一家就佔了 32.7% 的白字火力**，且與 `**` 藍字的三個來源群組（1/5/16）完全重疊——這是本文後段優化的核心。

---

## 三、主要結果（全保真模擬，150,000 次／策略）

模擬完整還原「群組不可重複」的兩階段抽取與逐轉計費（方法見附錄C），非公式近似。

| 路線 | 期望總點數 | 標準誤 | 中位數 | 90%分位 | 倍數 |
| --- | --- | --- | --- | --- | --- |
| **a）釘紫+藍，最後拼白** | 170,191 | ±384 | 126,000 | 362,000 | 1.10x |
| b）釘藍→加釘白，最後拼紫+白 | 247,655 | ±603 | 177,000 | 549,000 | 1.60x |
| c）先釘2白，最後拼紫+藍 | 447,131 | ±1,347 | 277,000 | 1,033,000 | 2.89x |
| **a★）a 的優化版（見七）** | **154,533** | ±334 | 118,000 | 322,000 | **1.00x** |

分布右偏明顯（平均 170,191 但中位數只要 126,000）——**抓預算建議看 90% 分位（362,000），不要只看平均值**，否則容易低估需要準備的點數。

### 為什麼 a 大幅領先

三條路線終局都是 5000 點／轉，勝負全看終局單轉成功率：

| 終局狀態 | 要同時中什麼 | 單轉成功率 |
| --- | --- | --- |
| 釘紫+藍（a） | 3 白中 2 個 `#` | **3.50%** |
| 釘紫+白 | `**`藍 + 2白中≥1個`#` | 2.99% |
| 釘藍+白（b） | 紫(1/9) + 2白中≥1個`#` | 2.14% |
| 釘白+白（c） | 紫(1/9) + `**`藍 | 1.31% |

原則：**該釘住的是最難的兩格**。紫字（11.1%）與藍字（18.4%）都是單格、無備援；白字雖然每格只有 14.7%，但 3 格輪流抽、只要 2 中，自帶備援（≥1 個命中的機率高達 37.9%）。a 把兩個最難、無備援的鎖住，讓最便宜、有備援的白字做「3 選 2」；c 反過來，把兩個最難的留到最後一起賭，等於把 1/9 × 18.4% 硬壓在同一轉上。

---

## 四、追加發現一：紫字這一格，值得釘

**單一釘位隔離測試**（各自單獨釘住，25,000 次）：

| 策略 | 期望點數 | 期望轉數 |
| --- | --- | --- |
| 完全不釘（2000/轉） | 2,805,215 | 1,403 轉 |
| **只釘紫（3000/轉）** | **485,103** | **162 轉** |
| 只釘藍（3000/轉） | 790,770 | 264 轉 |
| 只釘白（3000/轉，1格） | 976,442 | 326 轉 |

紫字 11.1% 看似三者最低，但它是唯一「單格無備援」中最難的，所以省下的期望轉數最多——釘紫讓單價漲 50%（2000→3000），卻讓期望轉數砍掉 88%。

**直接對照「允許 vs 禁止釘紫」**：

| | 期望點數 |
| --- | --- |
| 禁止釘紫的最佳解（只能藍+白） | 248,672 |
| 允許釘紫的最佳解 | 169,130 |

那一個釘位值 **79,500 點（省 32%）**。兩個釘位的最佳組合也印證同一件事：紫+藍（a，170,743）與紫+白（200,051）包辦前兩名，藍+白（b）與白+白（c）敬陪末座。

**小結**：優先序 **紫字 ＞ 藍字 ＞ 白字**。沒中紫字時建議整盤空洗（2000/轉），把釘位留給紫字，不要為了保留藍/白而多付釘選費——這個調整能再省約 1%（在雜訊範圍邊緣，順手做就好，不必勉強）。

---

## 五、追加發現二：白字群組互斥（縮池）效應

**問題**：白字選走特定群組後，藍字的候選池變少、機率上升的可能有沒有算進去？

**答案**：有算進去（模擬引擎本來就是重新歸一化的生成過程），而且效果**方向不對稱**——18 個群組裡，封鎖任一個對藍字命中 `**` 的影響：

| 封鎖的群組 | 藍字命中`**` | 變化 |
| --- | --- | --- |
| 群組6 | 22.94% | **+2.43pt ↑**（最大漲幅） |
| 群組17 / 10 | 22.09% / 22.08% | +1.57 / +1.56pt ↑ |
| 群組2,3,4,7,8,9,11,12,13,14,15 | 21.26～21.27% | +0.75pt ↑ |
| 群組18 | 20.99% | +0.48pt ↑ |
| 群組5 | 19.45% | −1.06pt ↓ |
| 群組1 | 17.61% | −2.90pt ↓ |
| **群組16** | **8.12%** | **−12.39pt ↓**（最大跌幅） |

（基準 20.51%，法A）18 個群組裡 **15 個是上升的**，只有 1、5、16 下降——因為 `**` 只住在這三個群組。

**但路線 c 剛好最容易踩到下降的那 3 個**：`#` 白字裡 67.4% 來自群組1/5/16。把「釘住的2個 `#` 白字」實際落點依權重算出來：

- 藍字機率**上升的機率只有 11.0%**，下降 89.0%
- 期望 `**` 機率從 20.51% 掉到 **11.86%**

而且「成本 = 1/機率」不是線性的，壞配置的懲罰被放大（Jensen 效應）：

```
期望終局成本 = 555,848 點
若用「期望機率11.86%」反推只有 379,425 點
```

差的 17.6 萬點，就是壞配置（例如群組1+16，終局只剩0.425%成功率）在期望值裡被放大的權重。**這才是路線 c 真正貴的原因**，比單看平均機率更嚴重。

**反方向也檢查了**：釘住 `**` 藍字後，白字池只會下降、不會上升——因為 `**` 只在群組1/5/16，而這三個群組同時是白字大戶：

| 藍字來自 | 白字`#`每格 | 變化 |
| --- | --- | --- |
| 群組5 | 14.11% | −0.60pt |
| 群組1 | 12.93% | −1.78pt |
| 群組16 | 11.57% | −3.14pt |

（基準14.71%）沒有上升分支，因為藍/白的推薦目標在群組16 是完全互斥的（見七）。

**抽取順序的敏感度**（藍字先抽 vs 白字先抽，25,000次）：

| 路線 | 藍先抽 | 白先抽 | 差異 |
| --- | --- | --- | --- |
| a | 169,858 | 168,889 | −0.6% |
| b | 246,989 | 247,770 | +0.3% |
| c | 444,815 | 471,210 | **+5.9%** |

只有 c 對順序敏感，且本文採用的順序對 c 較有利——排名結論不受此假設影響。

---

## 六、追加發現三：紫字有 1/9，值得釘嗎？

**是的，而且是三格裡最值得的一格。** 已在〔四〕用「禁止釘紫」的顯式對照證明（省 32%）。這裡補充直覺：1/9 看似偏高，但它比另外兩個對手更慘的地方在於「單格無備援」——藍字同樣單格無備援但機率更高（18.4%），白字雖然每格才14.7%卻有 3 格輪流抽、只要2中的容錯。三者放在一起比較，紫字才是最該優先鎖住的。

---

## 七、追加發現四：藍字該不該接受群組16？

群組16 的推薦項目彼此互斥（同一顆石頭不可能同時出現）：

| 稀有度 | 內容（4條技能線 × 對應數值） |
| --- | --- |
| `**`藍字（4項） | 果凍魔法師／閃避大師 餅乾傷害量增加**50%**、2個以上時增加**50%** |
| `#`白字（8項） | 同 4 條技能線的 **30% / 40%** 版本 |

釘住群組16 的 `**` 藍字，會把 8 個 `#` 白字（32.7% 的白字火力）一起排除，白字每格從 14.71% 掉到 11.57%。

**120,000 次／策略、附標準誤的對照**：

| 策略 | 期望總點數 | 中位數 |
| --- | --- | --- |
| 有紫才釘，藍白皆不挑群組（基準） | 168,311 ±423 | 125,000 |
| 只有白字避開群組16 | 167,598 ±431 | 123,000 |
| **藍字避開群組16** | **157,755 ±379** | 121,000 |
| 藍、白都避開群組16 | 154,693 ±376 | 118,000 |
| 上一項 + 沒白可釘時仍接受群組16藍 | 154,601 ±374 | 118,000 |

相對基準的節省（95% 信賴區間）：

| 策略 | 節省 | 是否顯著 |
| --- | --- | --- |
| 只有白字避開群組16 | 713 ±1,183（0.4%） | ✗ 不顯著 |
| 藍字避開群組16 | 10,557 ±1,113（6.3%） | ✓ 顯著 |
| 藍、白都避開群組16 | 13,618 ±1,109（**8.1%**） | ✓ 顯著 |

**關鍵：單獨避開群組16 的白字沒有用**，要先「藍字避開群組16」，白字的選擇才開始有價值——因為只有當你開始拒絕釘群組16 的藍字，你才會頻繁地面臨「釘哪個白字」的抉擇，這時避開群組16 的白字才有意義（交互作用，不是各自獨立的效果）。

**重要但書**：這條優化只適用於「任意 `**` 藍字都算贏」的目標。如果實際需求是**指定**要群組16 的某條特定藍字（例如「2個閃避大師以上時餅乾傷害量增加50%」），上述分析不適用——必須接受釘住群組16、白字只能從群組1/5/9/10/15 的 15 個 `#` 裡湊。

---

## 八、最終建議

1. **走路線 a**：釘紫+藍，重骰白字拼 2 個推薦白字。比 b 快 1.6 倍、比 c 快 2.9 倍。
2. **沒中紫字時整盤空洗**（2000點/轉），不要為了保留藍/白多付釘選費。
3. **藍字若剛好是群組16 的 `**`，先別急著釘**——留給第二釘位去挑白字，等白字湊到 1 個推薦項後，若藍字仍是群組16 也無妨（接受與否對結果無顯著差異）。
4. **預算抓 90% 分位（約 322,000～362,000 點）**，不要只用平均值規劃，因為花費分布右偏、中位數遠低於平均值。

按 1–3 執行後，期望總花費約 **154,500 點**（中位數 118,000）。

---

## 附錄 A：`**`/`#` 推薦項清單

**推薦藍字 `**`（9項）：**

| 群組 | 項目 |
| --- | --- |
| 1 | 餅乾造成的傷害量增加41-50% |
| 5 | 黃色/紅色/藍色技能傷害量增加41-50%、出現更多技能能量果凍 |
| 16 | 果凍魔法師餅乾傷害量增加50%、閃避大師餅乾傷害量增加50%、2個果凍魔法師以上時餅乾傷害量增加50%、2個閃避大師以上時餅乾傷害量增加50% |

**推薦白字 `#`（23項，依群組）：** 群組1（2項）、群組5（9項）、群組9（1項）、群組10（1項）、群組15（2項）、群組16（8項，見七）。完整清單見 `遊戲參數整理.txt` 中標 `#` 的項目，或 `script.js` 的 `RECOMMENDED_ITEMS_BY_GRADE`。

## 附錄 B：資料限制

群組3 的項目機率合計僅 62.12%（截圖未完整揭露），但該群組沒有任何 `#`／`**` 標記項目，只影響白字池歸一化時的「雜訊比例」——本文對白字命中率做過 13.57%/13.81%/14.71% 三種假設的敏感度測試，三路線排名均不變。沿用專案原則，不自行推測補齊未顯示的資料。

## 附錄 C：模擬方法與驗證腳本

**方法**：全保真 Monte Carlo，每一轉都用「先依群組機率抽群組（不可重複）、再於群組內依項目機率抽項目」的兩階段流程產生 1 紫 + 1 藍 + 3 白，逐轉依已釘選格數計費（2000/3000/5000），直到終局條件（紫中目標 且 藍中`**` 且 白字≥2個`#`）達成為止，取多次模擬的平均總花費。以下為可重跑的 node 腳本骨架（需搭配 `遊戲參數整理.txt` 同目錄執行 `node 檔名.js`）：

```js
"use strict";
const fs = require("fs");
// 解析 遊戲參數整理.txt：取得每個群組的機率(groupRate)，
// 以及每個群組內項目的 {group, rate, grade(high/mid/low), rec(是否為 **/# 推薦項)}
const raw = fs.readFileSync("遊戲參數整理.txt", "utf8");
const groupRate = new Map();
for (const l of raw.split(/\r?\n/)) {
  const m = l.match(/^附加能力群組\s*(\d+)\s*\|\s*([\d.]+)%/);
  if (m) groupRate.set(+m[1], +m[2] / 100);
}
const items = []; let cur = null;
for (const l of raw.split(/\r?\n/)) {
  const g = l.match(/^\[附加能力群組\s*(\d+)\]/);
  if (g) { cur = +g[1]; continue; }
  if (cur === null) continue;
  const m = l.match(/^(\*\*|\*|#)?\s*(.+?)\s*\|\s*([\d.]+)%\s*$/);
  if (!m || /^附加能力群組/.test(m[2])) continue;
  const k = m[1] || "";
  items.push({
    group: cur, rate: +m[3] / 100,
    grade: cur === 19 ? "high" : (k === "*" || k === "**") ? "mid" : "low",
    rec: k === "**" || k === "#",
  });
}
const GROUPS = [...groupRate.keys()].filter((g) => g <= 18);
const midBy = new Map(GROUPS.map((g) => [g, items.filter((i) => i.group === g && i.grade === "mid")]));
const lowBy = new Map(GROUPS.map((g) => [g, items.filter((i) => i.group === g && i.grade === "low")]));

function pick(list, weightOf) {
  let total = 0; for (const x of list) total += weightOf(x);
  let r = Math.random() * total;
  for (const x of list) { r -= weightOf(x); if (r <= 0) return x; }
  return list[list.length - 1];
}
// 兩階段抽取：先在未占用群組中依群組機率抽群組，再於群組內依項目機率抽項目
function drawSlot(grade, used) {
  const by = grade === "mid" ? midBy : lowBy;
  const pool = GROUPS.filter((g) => !used.has(g) && by.get(g).length > 0);
  const g = pick(pool, (x) => groupRate.get(x));
  used.add(g);
  return { group: g, rec: pick(by.get(g), (x) => x.rate).rec };
}

const COST = [2000, 3000, 5000];
// 路線 a：釘紫+藍，最後拼白
const policyA = (board) => ({ P: board.P, B: board.B.rec, W: [] });

function runOnce(policy) {
  let pin = { P: false, B: false, W: [] };
  let board = { P: false, B: { rec: false, group: null }, W: [null, null, null] };
  let cost = 0;
  for (let roll = 0; roll < 500000; roll += 1) {
    cost += COST[(pin.P ? 1 : 0) + (pin.B ? 1 : 0) + pin.W.length];
    const used = new Set([19]); // 紫字固定占用群組19
    if (pin.B) used.add(board.B.group);
    for (const i of pin.W) used.add(board.W[i].group);
    const next = { P: pin.P || Math.random() < 1 / 9, B: pin.B ? board.B : drawSlot("mid", used), W: [null, null, null] };
    for (let i = 0; i < 3; i += 1) next.W[i] = pin.W.includes(i) ? board.W[i] : null;
    for (let i = 0; i < 3; i += 1) if (!next.W[i]) next.W[i] = drawSlot("low", used);
    board = next;
    if (board.P && board.B.rec && board.W.filter((x) => x.rec).length >= 2) return cost;
    pin = policyA(board); // 換成其他路線的釘選函式即可比較
  }
  return cost;
}

const N = 150000;
const costs = Array.from({ length: N }, runOnce);
const mean = costs.reduce((a, b) => a + b, 0) / N;
console.log("期望總點數：", Math.round(mean).toLocaleString("en-US"));
```

三條路線與優化版的釘選函式（把 `policyA` 換成對應版本即可重算本文任何一個數字）：

```js
// b) 釘藍→加釘白，最後拼紫+白
const policyB = (b) => b.B.rec
  ? { P: false, B: true, W: b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i).slice(0, 1) }
  : { P: false, B: false, W: [] };

// c) 先釘2白，最後拼紫+藍
const policyC = (b) => ({ P: false, B: false, W: b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i).slice(0, 2) });

// a★) 優化版：沒中紫字不釘；藍字避開群組16；白字沒有非群組16的#時才退而求其次
const policyAStar = (b) => {
  if (!b.P) return { P: false, B: false, W: [] };
  if (b.B.rec && b.B.group !== 16) return { P: true, B: true, W: [] };
  const cands = b.W.map((x, i) => [x, i]).filter(([x]) => x.rec);
  const non16 = cands.filter(([x]) => x.group !== 16);
  return { P: true, B: false, W: (non16.length ? non16 : cands).slice(0, 1).map(([, i]) => i) };
};
```
