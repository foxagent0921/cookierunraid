# 推薦能力洗法分析：釘紫+藍 vs 釘藍+白 vs 釘白+白

打怪賽雕刻召喚石只能釘選 2 格、其餘 3 格必須重骰。本文比較三種釘法在「**任意**推薦能力」這個目標下的期望花費（點數），資料來源為 `遊戲參數整理.txt`（14 張遊戲截圖整理），機率標記依 `script.js` 的 `RECOMMENDED_ITEMS_BY_GRADE`。

**結論：方案 a 大幅勝出，且可再優化 2.0%。**

> 🔴 **2026-08-16：推薦白字從 27 項減為 22 項後全面重算，〔七〕的結論整個反轉。** 群組16 的 `#` 白字由 9 項縮為 4 項（只保留「閃避大師餅乾傷害量增加30%／40%」與「2個閃避大師以上時餅乾傷害量增加30%／40%」），移除的是果凍魔法師的 30%／40%、2個果凍魔法師以上的 30%／40%，以及閃避大師的 20%。**三條路線的排名不變（a ＜ b ＜ c），但群組16 的取捨規則從「藍字先別急著釘」翻成「藍字一定要釘」**：
>
> - **舊規則現在是負優化**：「藍字避開群組16」由省 2.3% 變成**多花 8.0%**（11,585 ±709，CRN 配對）。舊的 a★ 策略（藍白都避開群組16）也從省 6.4% 變成多花 5.3%。
> - **新的最佳解是「只在白字端避開群組16」**，省 **2.0%**（2,949 ±244）——這一條方向不變，因為它的效益來自「別讓白字釘走藍字的最大來源」，與 `#` 的數量無關。
> - 白字每格 16.14% → **13.85%**，路線 a 130,748 → **144,810**（變貴）。
> - **路線 c 反而變便宜**（495,845 → **407,652**）：推薦白字不再集中在群組16，c 釘住的 2 白比較不會封死 `**` 藍字，終局單轉從 1.02% 回升到 **1.64%**。c 與 a 的差距從 3.8 倍縮到 **2.8 倍**。
> - 〔五〕的代價排序**翻面**：釘住 `**` 藍字對白字的傷害，群組16 從最大（−3.36pt）變成最小（**−0.46pt**），群組1（−1.76pt）與群組5（−1.62pt）反而成了最貴的兩個。
> - 〔八〕的「不要釘 2 白」從 1% 級的差距縮到 **0.6% 以內**，四個第一手已幾乎等價。
>
> ⚠️ **2026-08-06：推薦白字從 24 項增為 27 項後全面重算**（新增群組5 的「出現技能能量果凍」「紅色技能能量獲得量+7-9%」與群組16 的「閃避大師餅乾傷害量增加20%」）。當時白字每格 14.93% → 16.14%、路線 a 144,681 → 130,748、〔七〕優化幅度 8.2% → 6.4%。
>
> ⚠️ **2026-08-04（第二次）：推薦白字從 23 項增為 24 項後全面重算**（新增群組10 的「體力恢復量增加11-15%」）。當時白字每格 13.72% → 14.93%、路線 a 175,425 → 144,681、〔七〕優化幅度 11.3% → 8.2%。
>
> ⚠️ **2026-08-04（第一次）：已依修正後的群組權重模型全面重算**（修正內容見 [釘選策略評估驗證紀錄.md](釘選策略評估驗證紀錄.md) 第二節）。修正的重點：抽群組時，群組機率必須先乘上「該群組真的吐出該等級的比例」再正規化。
> 本文舊版〔二〕列出的「法A：綜合權重（群組機率×項目機率）直接歸一化」其實接近正確做法，卻被判定為「粗略近似」而採用了法B——**當時的蒙地卡羅之所以支持法B，是因為模擬本身也用了同一個錯誤的群組抽法**，所以驗不出問題。

> 本文與 [釘選策略分析.md](釘選策略分析.md) 比較的是**同一組路線代號 a/b/c，但目標定義不同、結論相反**——那份文件的目標是兩個**指定**的白字數值格（每格約 0.29%～1.87%），本文的目標是「任意」推薦能力（白字每格約 13.9%、藍字約 19.8%）。兩者並不矛盾，差異純粹來自目標機率相差十幾倍。

---

## 一、問題設定

301 階以上召喚石的 5 個附加能力欄位固定為 1 紫 + 1 藍 + 3 白（欄位定義同 [釘選策略分析.md](釘選策略分析.md) 一節）。

**目標**（對應原始提問的三種洗法）：

- **紫字**：必出 1 個（群組19），中目標項機率 = **1/9 ≈ 11.11%**
- **藍字**：必出 1 個，目標 = 9 個標記 `**` 的推薦藍字之一
- **白字**：3 格中要湊到 **2 個**標記 `#` 的推薦白字（22 項之一）

**釘選費用**：洗 0 個釘選 2000 點／轉，釘 1 個 3000 點／轉，釘 2 個 5000 點／轉。

**三種路線**（對應原提問 a/b/c）：

- **a）** 釘紫字＋藍字，重骰 3 白，拼出 2 個推薦白字
- **b）** 釘藍字，先在白字裡拼到 1 個推薦白字並釘住，再重骰「紫字＋剩下 1 個白字」
- **c）** 先在白字裡拼到 2 個推薦白字都釘住，再重骰「紫字＋藍字」

---

## 二、三個關鍵機率

### 紫字：1/9 ≈ 11.11%

群組19（`HIGH_GRADE_GROUP_ID`）固定 9 個項目、各 11.11%，題目已直接給定。

### 藍字：≈ 19.8%

藍字池為群組1–18 的 44 個 `*` 項目，其中 9 個標 `**`：

| 算法 | 藍字命中推薦(`**`)機率 |
| --- | --- |
| ❌ 舊模型：依原始群組機率抽群組，再於群組內藍字項目歸一化 | 18.45% |
| ✅ **修正後：群組權重＝群組機率 × 該群組中級占比（模擬，本文採用）** | **19.78%** |
| 同上，單格封閉式（僅排除群組19） | 19.82% |
| 參考：群組3 補齊前的同口徑數值 | 20.18% / 20.08% |

舊模型的問題是把「**含有**中級項目的群組」一視同仁，但藍字格保證是中級，而群組11/12/17/18 的項目全是中級（抽中必定是中級）、群組5 只有 2.60% 是中級。權重必須按這個比例縮放。

**藍字這一節完全不受 2026-08-16 的資料異動影響**——變動的 5 個標記都是 `#`（白字），`**` 仍是 9 項。與上一版列出的 20.18%／20.08% 的差距不是雜訊，而是 2026-08-14 補齊群組3 的效果：群組3 多了一個 `*` 項目，藍字權重上升，其餘群組同步略降約 1.3%（見附錄B）。上一版的表格未隨補齊更新，本次一併修正。這也是本次重跑的一個交叉檢查點。

9 個 `**` 藍字分布：群組1（1項）、群組5（4項）、群組16（4項）——**只集中在這三個群組**，這件事後面會反覆用到。

### 白字：每格 ≈ 13.9%

白字池為群組1–18 的低級項目，其中 22 個標 `#`（2026-08-16 把群組16 的 9 項收窄為 4 項）：

| 算法 | 白字每格命中推薦(`#`)機率 |
| --- | --- |
| 單格封閉式（僅排除群組19） | 13.81% |
| ✅ **模擬（含群組互斥，本文採用）** | **13.85%** |
| 封閉式＋藍字卡位＋3 格互斥 | 13.82% |
| 參考：27 項時的同口徑數值 | 16.14% |
| 參考：24 項時的同口徑數值 | 14.93% |

> **2026-08-11：封閉式解與模擬對上了。**把「藍字先抽並卡住一個群組」納入封閉式計算後，三種口徑會收斂到同一個值；原本只排除群組19 的單格封閉式在 27 項時明顯偏高（16.69% 對 16.14%），因為它假設白字面對完整的群組池。`script.js` 的 `getStoneHitRate` 原先走的正是那條路，已同步修正。**收窄到 22 項後三種口徑幾乎重合（13.81%／13.85%／13.82%）**——這不是巧合：偏差原本主要來自群組16 被藍字卡位，而群組16 的 `#` 變少後，它在白字火力裡的份量也跟著變小。

移除 5 項讓白字每格下降 2.29 個百分點（16.14% → 13.85%），是三次資料異動裡幅度最大的一次。

22 個 `#` 白字分布與火力（權重 × 群組內占比，封閉式單格口徑）：

| 群組 | `#` 項數 | 占推薦白字火力 | 27 項時 |
| --- | --- | --- | --- |
| 5 | 11 | **24.5%** | 20.2% |
| 16 | 4（−5） | **16.8%** | **31.2%** |
| 1 | 2 | 16.8% | 13.9% |
| 10 | 2 | 16.8% | 13.9% |
| 15 | 2 | 16.8% | 13.9% |
| 9 | 1 | 8.4% | 6.9% |

**群組16 從一枝獨秀掉到與群組1/10/15 並列**，火力腰斬（31.2% → 16.8%），群組5 接手成為最大來源。連帶地，`#` 白字落在「住著 `**` 藍字」的群組1/5/16 的比重從 65.3% 降到 **58.1%**——白字與藍字的目標不再那麼互相踩腳。後面〔三〕路線 c 變便宜、〔五〕代價排序翻面、〔七〕結論反轉，全都是這一件事的後果。

---

## 三、主要結果（全保真模擬，150,000 次／策略）

模擬完整還原「群組不可重複」的兩階段抽取與逐轉計費（方法見附錄C），非公式近似。

| 路線 | 期望總點數 | 標準誤 | 中位數 | 90%分位 | 倍數 |
| --- | --- | --- | --- | --- | --- |
| **a）釘紫+藍，最後拼白** | 144,810 | ±316 | 110,000 | 302,000 | 1.03x |
| b）釘藍→加釘白，最後拼紫+白 | 222,456 | ±536 | 159,000 | 492,000 | 1.58x |
| c）先釘2白，最後拼紫+藍 | 407,652 | ±1,469 | 227,000 | 931,000 | 2.89x |
| **a★）a 的優化版（見七）** | **141,179** | ±313 | 106,000 | 298,000 | **1.00x** |

分布右偏明顯（平均 144,810 但中位數只要 110,000）——**抓預算建議看 90% 分位（302,000），不要只看平均值**，否則容易低估需要準備的點數。

**這次移除 5 個 `#` 之後，a 與 b 變貴，c 卻變便宜了**：a 多花 10.8%、b 多花 5.0%、**c 省 17.8%**。方向與上一次（24→27 項）完全相反，機制也是同一個、只是反過來走：〔二〕那張火力表顯示推薦白字**不再集中**在「藍字大戶」群組16。a 的終局完全靠白字，白字變難就直接變貴；c 的終局是「紫 × 藍」，白字變難只拉長前置階段，但它釘住的 2 白現在比較不會封死 `**` 藍字的來源，兩相抵銷後 c 淨變便宜。c 與 a 的差距因此從 3.8 倍縮到 **2.8 倍**——但 c 仍然是最貴的一條。

### 為什麼 a 大幅領先

三條路線終局都是 5000 點／轉，勝負全看終局單轉成功率（下表為模擬中實際觀測到的各釘法終局命中率）：

| 終局狀態 | 要同時中什麼 | 單轉成功率 | 27 項時 |
| --- | --- | --- | --- |
| 釘紫+藍（a） | 3 白中 2 個 `#` | **4.11%** | 4.67% |
| 釘紫+白 | `**`藍 + 2白中≥1個`#` | 3.49% | 3.24% |
| 釘藍+白（b） | 紫(1/9) + 2白中≥1個`#` | 2.39% | 2.53% |
| 釘白+白（c） | 紫(1/9) + `**`藍 | **1.26%** | 1.01% |

（上表為**路線邊際化**口徑：在整條路線的模擬中，統計「已進入該終局釘選狀態」的每一轉實際完工的比例，因此已包含釘選格的真實落點分布。同一份腳本在 27 項設定下重跑可重現前一版的 1.01%／2.53%。）

原則：**該釘住的是最難的兩格**。紫字（11.1%）與藍字（19.8%）都是單格、無備援；白字雖然每格只有 13.9%，但 3 格輪流抽、只要 2 中，自帶備援。a 把兩個最難、無備援的鎖住，讓最便宜、有備援的白字做「3 選 2」；c 反過來，把兩個最難的留到最後一起賭，等於把 1/9 × 19.8% 硬壓在同一轉上。

**注意 c 那一列是往上走的（1.01% → 1.26%）**。它是唯一終局不含白字的路線，所以白字變難抽對它毫無傷害；反過來，推薦白字不再集中在群組16 之後，它釘住的那 2 格比較少把 `**` 藍字的來源封死——期望藍字命中率從 13.00% 回升到 **14.74%**（見〔五〕）。「釘紫+白」同理小漲（3.24% → 3.49%）：釘住的那格白字比較少是群組16，藍字就比較不會被卡。

**路線 a 的變貴幾乎完全來自終局**：終局單轉 4.67% → 4.11%（相對 −12.0%），反推 1/p 上升 13.6%，與實測的總花費 +10.8% 對得上。白字每格掉了 14.2%（16.14% → 13.85%），「3 選 2」的備援只吸收掉其中很小一部分——**備援能攤平運氣，不能補回機率**。

---

## 四、追加發現一：紫字這一格，值得釘

**單一釘位隔離測試**（各自單獨釘住，30,000 次）：

| 策略 | 期望點數 | 期望轉數 |
| --- | --- | --- |
| 完全不釘（2000/轉） | 2,210,769 ±12,692 | 1,105 轉 |
| **只釘紫（3000/轉）** | **384,950 ±2,125** | **128 轉** |
| 只釘藍（3000/轉） | 661,086 ±3,829 | 220 轉 |
| 只釘白（3000/轉，1格） | 797,209 ±5,303 | 266 轉 |

紫字 11.1% 看似三者最低，但它是唯一「單格無備援」中最難的，所以省下的期望轉數最多——釘紫讓單價漲 50%（2000→3000），卻讓期望轉數砍掉 88%。

**直接對照「允許 vs 禁止釘紫」**（各 60,000 次）：

| | 期望點數 |
| --- | --- |
| 禁止釘紫的最佳解（只能藍+白） | 225,996 ±862 |
| 允許釘紫的最佳解（紫+藍） | 144,791 ±498 |

那一個釘位值 **81,200 點（省 35.9%）**。這個比例的走勢是 30% → 37% → 39.6% → **35.9%**：前三次是白字愈容易湊、瓶頸愈集中在紫字，所以紫字那一格愈值錢；**這次白字變難，瓶頸部分移回白字，紫字的相對價值因此回落**。不過 35.9% 仍是壓倒性的差距，**「紫字優先」的結論方向完全不變**，只是領先幅度小了一點。

**小結**：優先序 **紫字 ＞ 藍字 ＞ 白字**。沒中紫字時建議整盤空洗（2000/轉），把釘位留給紫字，不要為了保留藍/白而多付釘選費——這個調整能再省約 1%（在雜訊範圍邊緣，順手做就好，不必勉強）。

---

## 五、追加發現二：白字群組互斥（縮池）效應

**問題**：白字選走特定群組後，藍字的候選池變少、機率上升的可能有沒有算進去？

**答案**：有算進去（模擬引擎本來就是重新歸一化的生成過程），而且效果**方向不對稱**——18 個群組裡，封鎖任一個對藍字命中 `**` 的影響：

| 封鎖的群組 | 藍字命中`**` | 變化 |
| --- | --- | --- |
| 群組6 | 22.07% | **+2.26pt ↑**（最大漲幅） |
| 群組3 / 10 | 21.27% / 21.27% | +1.45pt ↑ |
| 群組2,4,7,8,9,13,14,15 | 20.51～20.52% | +0.70pt ↑ |
| 群組5 | 18.73% | −1.09pt ↓ |
| 群組1 | 16.99% | −2.83pt ↓ |
| **群組16** | **7.77%** | **−12.04pt ↓**（最大跌幅） |

（基準 19.82%，封閉式）能被白字釘走的 14 個群組裡 **11 個是上升的**，只有 1、5、16 下降——因為 `**` 只住在這三個群組。群組11/12/17/18 全是中級項目，白字格永遠抽不到，所以不可能被白字釘走，本表不列。**這張表完全不受 2026-08-16 的資料異動影響**（變動的都是 `#` 白字）；與上一版的差異（基準 20.08% → 19.82%）只來自 2026-08-14 補齊群組3。

**路線 c 仍然最容易踩到下降的那 3 個，但踩到的頻率下降了**：`#` 白字火力有 **58.1%** 來自群組1/5/16（27 項時是 65.3%）。把「釘住的2個 `#` 白字」實際落點依權重算出來：

- 藍字機率**上升的機率提高到 23.6%**，下降 76.4%（27 項時是 17.8% / 82.2%）
- 期望 `**` 機率從 19.82% 掉到 **14.74%**（27 項時掉到 13.00%）

而且「成本 = 1/機率」不是線性的，壞配置的懲罰被放大（Jensen 效應）：

```
期望終局成本 E[1/p] = 428,710 點
若用「期望機率 14.74%」反推 1/E[p] 只有 305,227 點
```

差的 12.3 萬點，就是壞配置在期望值裡被放大的權重（27 項時是 15.9 萬點）。最常落到的組合已經不再全部含群組16：

| 釘住的白字群組 | 出現機率 | 終局單轉成功率 |
| --- | --- | --- |
| 群組5 + 10 | 10.5% | 2.250% |
| **群組5 + 16** | 10.4% | **0.536%**（最糟） |
| 群組1 + 5 | 10.4% | 1.730% |
| 群組5 + 15 | 10.4% | 2.162% |
| 群組1 + 15 | 6.8% | 1.957% |
| 群組15 + 16 | 6.7% | 0.902% |

**這仍是路線 c 貴的主因，但毒性明顯稀釋了。** 27 項時最常見的四種落點**全部**含群組16，最糟的「群組1+16」就佔 10.7%；現在含群組16 的組合退居次要，榜首換成無害的「群組5+10」（終局 2.250%，是最糟組合的 4.2 倍）。這就是 c 這次淨變便宜 17.8% 的直接原因。

**反方向也檢查了**：釘住 `**` 藍字後，白字池只會下降、不會上升——因為 `**` 只在群組1/5/16，而這三個群組同時是白字大戶：

| 藍字來自 | 白字`#`每格 | 變化 | 27 項時的變化 |
| --- | --- | --- | --- |
| 群組1 | 12.05% | **−1.76pt** | −1.62pt |
| 群組5 | 12.18% | −1.62pt | −1.14pt |
| 群組16 | 13.34% | **−0.46pt** | **−3.36pt** |

（基準 13.81%，封閉式）沒有上升分支，因為 `**` 藍字只住在這三個白字大戶群組。**這張表整個翻面了，而且是本次最重要的一個變化**：群組16 從代價最大（−3.36pt）變成代價最小（**−0.46pt**），群組1 與群組5 反而成了最貴的兩個。原因很直接——群組16 的 `#` 白字只剩 4 項、火力 16.8%，釘住它的藍字已經封不掉多少白字火力了；而群組1（4 個白字項目就有 2 個 `#`、群組內命中率 50%）與群組5（11 個 `#`）沒有變。

**這一格數字就是〔七〕結論反轉的來源。** 舊版「藍字先別急著釘群組16」的整個立論，建立在「釘它會賠掉 31.2% 的白字火力」上；那個前提現在不成立了。

**抽取順序的敏感度**（藍字先抽 vs 白字先抽，各 40,000 次）：

| 路線 | 藍先抽 | 白先抽 | 差異 |
| --- | --- | --- | --- |
| a | 145,034 ±609 | 147,992 ±615 | +2.0% |
| b | 222,962 ±1,050 | 226,759 ±1,043 | +1.7% |
| c | 410,711 ±2,860 | 460,483 ±3,223 | **+12.1%** |

只有 c 明顯對順序敏感——因為群組16 的藍字權重高，「白字先抽走群組16」對藍字的傷害最大。這個敏感度維持在 12.1%（27 項時也是 12.1%），因為它取決於群組16 的**藍字**權重，與 `#` 的數量無關。a 與 b 的差異雖然這次跨出了雜訊（+2.0%／+1.7%），量級仍遠小於路線之間的差距。本文採用的順序（藍先抽）對 c 較有利，所以排名結論不受此假設影響：即使用對 c 最有利的假設，它仍是三者中最貴的。

---

## 六、追加發現三：紫字有 1/9，值得釘嗎？

**是的，而且是三格裡最值得的一格。** 已在〔四〕用「禁止釘紫」的顯式對照證明（省 35.9%）。這裡補充直覺：1/9 看似偏高，但它比另外兩個對手更慘的地方在於「單格無備援」——藍字同樣單格無備援但機率更高（19.8%），白字雖然每格才 13.9% 卻有 3 格輪流抽、只要 2 中的容錯。三者放在一起比較，紫字才是最該優先鎖住的。推薦白字收窄為 22 項後白字變難湊，瓶頸略微移回白字（省下的比例從 39.6% 回落到 35.9%），但差距仍然懸殊，結論不變。

---

## 七、追加發現四：群組16 的取捨（2026-08-16 結論反轉）

群組16 的推薦項目彼此互斥（同一顆石頭不可能同時出現）：

| 稀有度 | 內容 |
| --- | --- |
| `**`藍字（4項） | 果凍魔法師／閃避大師 餅乾傷害量增加**50%**、2個以上時增加**50%**（4 條技能線） |
| `#`白字（4項） | **只剩閃避大師那 2 條**技能線的 **30% / 40%** 版本 |

**互斥的規模不對等了。** 釘住群組16 的 `**` 藍字，現在只會排除 4 個 `#` 白字（16.8% 的白字火力），白字每格從 13.81% 掉到 **13.34%**——只賠 0.46pt。反過來，用白字釘走群組16，仍然會把藍字命中 `**` 的機率從 19.82% 砸到 **7.77%**（−12.04pt），因為 9 個 `**` 裡有 4 個住在這裡，而且群組16 是藍字格權重最高的群組。

**120,000 次／策略、CRN 配對的對照**（同一批亂數餵給每個策略，逐樣本相減）：

| 策略 | 期望總點數 | 中位數 |
| --- | --- | --- |
| 有紫才釘，藍白皆不挑群組（基準） | 144,077 ±358 | 108,000 |
| **只有白字避開群組16** | **141,128 ±351** | 106,000 |
| 藍字避開群組16 | 155,662 ±393 | 115,000 |
| 藍、白都避開群組16（舊 a★） | 151,671 ±386 | 111,000 |
| 藍、白都避開群組1 | 143,286 ±354 | 108,000 |
| 藍、白都避開群組5 | 144,184 ±358 | 108,000 |

相對基準的節省（95% 信賴區間，配對法）：

| 策略 | 節省 | 是否顯著 |
| --- | --- | --- |
| **只有白字避開群組16** | **2,949 ±244（2.0%）** | ✓ 顯著 |
| 藍字避開群組16 | **−11,585 ±709（−8.0%）** | ✓ 顯著**變貴** |
| 藍、白都避開群組16（舊 a★） | −7,594 ±740（−5.3%） | ✓ 顯著**變貴** |
| 藍、白都避開群組1 | 791 ±406（0.5%） | ✓ 顯著但幅度極小 |
| 藍、白都避開群組5 | −107 ±362（−0.1%） | ✗ 雜訊內 |

**舊的 a★ 策略現在是負優化。** 「藍字避開群組16」從省 2.3% 變成多花 8.0%，是本次重跑裡最大的一個翻轉。道理很簡單：拒絕一個已經到手的 `**` 藍字，代價是把一個 19.8% 難度、單格無備援的格子丟回池子；而換來的好處——保住群組16 的白字火力——已經從 3.36pt 縮到 0.46pt，完全不夠付。

**兩個方向的效益是分開的，不要一起套用：**

- **白字端規則不變、方向也不變**：釘白字時避開群組16，省 2.0%。它的效益來自「別讓白字釘走藍字的最大來源」，取決於群組16 的**藍字**權重，與 `#` 數量無關，所以這條在三次資料異動裡都成立。
- **藍字端規則反轉**：`**` 藍字不論來自哪個群組，到手就釘。避開群組1 雖然統計上顯著（省 0.5%），但幅度只有白字端的四分之一、且需要記一條額外規則，實務上不值得。

**同一支腳本、同一組策略定義在 27 項與 22 項下各跑一次**（避免跨版本定義不一致）：

| 策略 | 27 項 | 22 項 |
| --- | --- | --- |
| 只有白字避開群組16 | 2.3% | **2.0%** |
| **藍字避開群組16** | **+2.1%** | **−8.0%** |
| 藍、白都避開群組16 | 6.3% | −5.3% |

左欄用 27 項設定跑出的 2.3%／2.1%／6.3%，與前一版記錄的 2.2%／2.3%／6.4% 一致，證明腳本本身沒變、翻轉純粹來自資料。

**重要但書**：以上都以「任意 `**` 藍字都算贏」為目標。如果實際需求是**指定**要群組16 的某條特定藍字（例如「2個閃避大師以上時餅乾傷害量增加50%」），這一節不適用——必須接受釘住群組16，白字只能從群組1/5/9/10/15 的 18 個 `#` 裡湊。反過來，如果你指定要的是群組16 那 4 個 `#` 白字之一，請看 [釘選策略分析.md](釘選策略分析.md)：那裡的目標是指定數值格，結論與本文不同。

---

## 八、追加發現五：盤面已有「1 藍 + 2 白」時，該釘哪兩格？

前面比較的是三條**路線**，這一節處理實戰中最常被問到的**單一決策點**。

**情境**：某一轉開出 **紫字沒中、藍字是 `**` 推薦、3 格白字裡恰好 2 個是 `#` 推薦**。只差紫字就完工（紫字若也中就直接結束了）。兩個候選第一手，**都是 5000 點／轉**：

- **釘 藍+1白** — 重骰 紫 + 2 白，完工需要 紫(1/9) **且** 2 白中至少 1 個 `#`
- **釘 2白** — 重骰 紫 + 藍 + 白，完工需要 紫(1/9) **且** 藍字中 `**`

**結論：四個第一手已經幾乎等價，差距全在 0.6% 以內。** 舊版「不要釘 2 白」的 1% 級差距被壓縮掉了，這條規則現在只剩方向、沒剩幅度。

### 1｜單轉完工率

600,000 次條件式模擬（起始盤面以拒絕取樣抽出，符合上述條件）：

| 盤面分支 | 出現率 | 釘 藍+1白 | 釘 2白 | 倍數 |
| --- | --- | --- | --- | --- |
| 藍字來自群組16 | **72.6%** | 2.447% | 2.136% | 1.15x |
| 藍字非群組16 | 27.4% | 2.248% | 1.640% | **1.37x** |
| 合計 | 100% | **2.392%** | 2.000% | 1.20x |

**兩個分支都被拉近了**（27 項時是 1.14x / 2.18x / 1.41x）。非群組16 分支從 2.18x 掉到 1.37x 是主因：釘 2 白時，被釘走的白字現在比較少落在群組16，重骰的藍字因此比較不會被封死，「釘 2 白」這一手就沒那麼糟了。

**群組16 分支的出現率從 63.9% 升到 72.6%**，也是同一件事的反面：藍字佔著群組16 時，被封掉的 `#` 白字從 9 個減為 4 個，「3 白中恰好 2 個推薦」變得更容易同時成立，這個分支自然更常見。

### 2｜為什麼釘 2 白仍然（略微）落後

同樣是丟掉一格已經到手的推薦項，**補回來的難度仍有差**，只是差距縮小了：

| 丟掉的東西 | 能補的格數 | 補回機率 | 27 項時 |
| --- | --- | --- | --- |
| 藍字（釘 2白 的代價） | 1 格，沒有備援 | **18.0%** | 16.5% |
| 1 個白字（釘 藍+1白 的代價） | 2 格輪流抽 | **21.5%** | 23.4% |

> **2026-08-11 修正（仍適用）：**這兩個補回機率直接由模擬完工率換算（`2.392% ÷ 1/9 = 21.5%`、`2.000% ÷ 1/9 = 18.0%`），不用 `1 − (1−p)²` 的獨立近似——兩個白字格會互相排除群組並不獨立，而且 p 也不是一般盤面的每格機率。

白字每格在這個情境只剩約 **11.3%**（兩個群組已被釘走，實測值），但**天生有備援**（3 格湊 2，還剩 2 格輪流抽，合起來 21.5%）；藍字只有 1 格、沒有備援，只有 18.0%。方向仍然符合〔三〕「**該釘住的是最難的兩格**」，但兩者已經很接近：推薦白字收窄後白字的備援變薄（23.4% → 21.5%），而藍字端因為群組16 較少被白字封鎖反而變好（16.5% → 18.0%），兩邊一起往中間靠。

### 3｜整段期望花費

上面是單轉效率。但這顆石頭還要洗很久，真正該比的是**從此盤面到完工的期望總花費**。用同一批起始盤面配對、第一手強制指定、之後一律套用〔七〕的新最佳策略（藍字照釘、白字避開群組16）：

| 第一手 | 期望總花費 | 與「釘 2白」的配對差 |
| --- | --- | --- |
| **整盤空洗（2000/轉）** | **140,697 ±242** | **−836 ±639**（95% CI，顯著） |
| 只釘 藍（3000/轉） | 141,132 ±242 | −400 ±644（雜訊內） |
| 釘 藍+1白 | 141,253 ±243 | −280 ±181（顯著） |
| 釘 2白 | 141,533 ±243 | — |

（CRN 配對 250,000 次）**四個選項的全距只有 836 點、約 0.6%**，比上一版的 1% 又更小。排序仍是「釘 2 白」墊底，但它與「釘 藍+1白」的差距只剩 280 點，實務上不值得為此改變操作習慣。原因和上一版相同：瓶頸仍然是紫字的 1/9，紫字沒中時怎麼釘都得等，第一手的影響很快被後續續盤吸收掉。

### 4｜實務做法

**本節現在沒有任何強結論——四個第一手都可以，挑順手的就好。**

1. **整盤空洗略勝**（比釘 2 白省 836 ±639），也最省事，與〔九〕第 2 條一致。
2. **「釘 2 白」只落後 0.6%**，不再是明確要避開的選項；若你偏好保住手上的兩個白字，直接釘也沒關係。
3. **若要釘白字，挑非群組16 的那個**（理由同〔七〕，這條規則沒變）。**藍字則不論來自哪個群組都可以釘**——這與舊版「群組16 藍字先別急著釘」相反，見〔七〕。
4. **別為這個決定糾結**：整段只差 0.6%，真正的分水嶺仍然是紫字有沒有中。

> **這一節沒有推翻〔四〕〔六〕的「紫字優先」。** 那條優先序講的是「**手上已經有**這一格想要的內容時，釘住它值多少」；本節的前提是**紫字沒中**，根本不能釘，所以紫字不在候選名單裡。兩條規則是接力關係：**紫中了就釘紫+藍**（終局 4.11%／轉，全場最強）；**紫沒中就照本節處理**。續盤策略正是這樣實作的——`if (!b.P) return { P:false, B:false, W:[] }`，紫沒中就整盤不釘、把釘位留給紫字。

> **與〔三〕路線表的數字對照**：〔三〕列出「釘白+白」的終局單轉成功率只有 1.26%，本節卻是 2.00%，兩者不衝突。前者是**整條路線邊際化**的結果（逐轉「先到先釘」慢慢累積 2 白，落點偏向群組16）；本節條件在「**單轉同時開出 2 個 `#` 白**」，落點平均得多（含群組16 的比例只有 **11.5%**，27 項時是 22.7%），所以成功率較高。推薦白字收窄後兩者的落差反而縮小了（1.02% vs 1.84% → 1.26% vs 2.00%），因為路線邊際化那一側改善得比較多。

---

## 九、最終建議

1. **走路線 a**：釘紫+藍，重骰白字拼 2 個推薦白字。比 b 快 1.6 倍、比 c 快 2.8 倍。
2. **沒中紫字時整盤空洗**（2000點/轉），不要為了保留藍/白多付釘選費。
3. **`**` 藍字到手就釘，不必挑群組**。**這一條在 2026-08-16 反轉**：舊版建議「群組16 的藍字先別急著釘」，在新資料下會多花 8.0%。群組16 現在只剩 4 個 `#` 白字，釘住它的藍字只賠 0.46pt 的白字火力，遠不值得為此放掉一個 19.8% 難度、單格無備援的欄位。
4. **要釘白字時，優先挑非群組16 的那一個**（省 2.0%）。這條規則方向不變——它防的是「白字把藍字的最大來源鎖死」，與 `#` 有幾項無關。
5. **紫字沒中、盤面卻已有 1 藍 + 2 白時：四種第一手都可以**（全距只有 0.6%）。整盤空洗略勝、釘 2 白墊底，但差距已小到不值得為它改變操作習慣。注意這一條**不是**「紫字優先」的例外——紫字沒中就不能釘，本來就不在選項裡。詳見〔八〕。
6. **預算抓 90% 分位（約 298,000～302,000 點）**，不要只用平均值規劃，因為花費分布右偏、中位數遠低於平均值。

按 1–5 執行後，期望總花費約 **141,000 點**（中位數 106,000）。

---

## 附錄 A：`**`/`#` 推薦項清單

**推薦藍字 `**`（9項）：**

| 群組 | 項目 |
| --- | --- |
| 1 | 餅乾造成的傷害量增加41-50% |
| 5 | 黃色/紅色/藍色技能傷害量增加41-50%、出現更多技能能量果凍 |
| 16 | 果凍魔法師餅乾傷害量增加50%、閃避大師餅乾傷害量增加50%、2個果凍魔法師以上時餅乾傷害量增加50%、2個閃避大師以上時餅乾傷害量增加50% |

**推薦白字 `#`（22項，依群組）：** 群組1（2項）、群組5（**11項**）、群組9（1項）、群組10（2項：體力恢復量增加11-15%／16-20%）、群組15（2項）、群組16（**4項**，見七）。2026-08-16 從群組16 移除 5 項（果凍魔法師餅乾傷害量增加30%／40%、2個果凍魔法師以上時餅乾傷害量增加30%／40%、閃避大師餅乾傷害量增加20%），只保留閃避大師那兩條技能線的 30%／40%。完整清單見 `遊戲參數整理.txt` 中標 `#` 的項目，或 `script.js` 的 `RECOMMENDED_ITEMS_BY_GRADE`。

## 附錄 B：資料限制

群組3 的截圖只揭露到 62.12%，2026-08-14 依群組10 的同型結構推論補齊 4 項（「餅乾所受傷害量減少」11-20%／21-30%／31-40% 各 12.12% 為白字、41-50% 為 1.52% 藍字），補齊後合計 100.00%。該群組仍沒有任何 `#`／`**` 標記項目（2026-08-16 的異動全部落在群組16，不改變這一點），對本文而言只是白字池與藍字池裡的「雜訊」，不影響三路線的排名。**2026-08-16 重跑起，〔二〕〔五〕的藍字數字已全面改用補齊後的口徑**（基準 20.08% → 19.82%）；先前版本的表格停留在補齊前的數值，本次一併修正。

補齊改變了雜訊的比重，方向相反的兩件事：**白字**方面，群組3 的低級占比從補齊前正規化出來的 97.55% 回到 96.96%，白字格抽中群組3 由 9.3266% 降為 9.2751%，稀釋略減；**藍字**方面，群組3 多了一個 `*` 項目，藍字項目合計占比從 1.52/62.12 ＝ 2.45% 升到 3.04%，群組3 的藍字權重上升約 24%，其餘群組的藍字機率同步略降（例如群組16 由 20.72% 降為 20.44%、群組5 由 9.16% 降為 9.04%）。推薦藍字的絕對命中率因此各降約 1.3%（例如群組1 的 `**` 由 3.4497% 降為 3.4041%），但九個 `**` 同向等比變動，相對排名不變。這 4 項為推論值而非截圖實測值。

## 附錄 C：模擬方法與驗證腳本

**方法**：全保真 Monte Carlo，每一轉都用「先抽群組（不可重複）、再於群組內依項目機率抽項目」的兩階段流程產生 1 紫 + 1 藍 + 3 白，逐轉依已釘選格數計費（2000/3000/5000），直到終局條件（紫中目標 且 藍中`**` 且 白字≥2個`#`）達成為止，取多次模擬的平均總花費。

**2026-08-16 重跑**：本文所有模擬數字已在推薦白字收窄為 22 項後重跑。依慣例先做重現檢查——把移除的 5 個 `#` 旗標加回來、還原成 27 項設定，看能否重現前一版的數字：

| 項目 | 27 項設定重現值 | 前一版記錄 | 判定 |
| --- | --- | --- | --- |
| 白字每格命中 `#`（模擬） | 16.17% | 16.14% | ✓ 雜訊內 |
| 路線 a 期望總點數 | 130,316 ±278 | 130,748 ±281 | ✓ 雜訊內 |
| 路線 b 期望總點數 | 210,320 ±507 | 211,829 ±512 | ✓ 低 0.7%（約 2σ） |
| 路線 c 期望總點數 | 502,052 ±1,744 | 495,845 ±1,739 | ⚠ 高 1.25%（見下） |
| 〔三〕終局：釘白+白 | 1.01% | 1.02% | ✓ |
| 〔三〕終局：釘藍+白 | 2.53% | 2.51% | ✓ |
| 〔七〕基準 | 131,515 ±313 | 131,206 ±313 | ✓ |
| 〔七〕只有白字避開群組16 | 省 2.3% | 省 2.2% | ✓ |
| 〔七〕藍字避開群組16 | 省 2.1% | 省 2.3% | ✓ |
| 〔七〕藍、白都避開群組16 | 省 6.3% | 省 6.4% | ✓ |
| 〔八〕起始盤面含群組16 | 22.7% | 22.7% | ✓ |

**三點要說清楚**：

1. **路線 c 高出 1.25% 是預期內的，來源已定位**：2026-08-14 補齊群組3 後，藍字多了一個競爭群組，`**` 命中率整體下降約 1.3%（見附錄B）。c 是唯一終局純靠「紫 × 藍」的路線，所以只有它會等比變貴。其餘路線的終局含白字，受影響很小，重現值都落在雜訊內。這同時也解釋了〔二〕藍字表格從 20.08% 改為 19.82%。
2. **上一版「對不上」的那一列（只有白字避開群組16）這次對上了**（2.3% vs 前一版重跑記錄的 2.3%）。該策略定義已寫進本次腳本（`makeAvoid(group, avoidBlue, avoidWhite)`），不再需要照文字敘述重新實作。
3. **本次改用 CRN（共用亂數）配對**：每個樣本用同一個種子餵給所有策略，再逐樣本相減。〔七〕的信賴區間因此從 ±865 收斂到 ±244，才有辦法分辨 0.5% 等級的差距（例如「藍、白都避開群組1」的 791 ±406）。

腳本本身未改動——`items` 的 `rec` 旗標直接讀文字檔的 `#`／`**` 標記，所以文字檔改標記後重跑即可。

**2026-08-04（第二次）重跑**：推薦白字由 23 項增為 24 項後重跑；重跑前已用 23 項設定驗證腳本能重現前一版的數字（白字每格 13.73% 對 13.72%、路線 a 176,513 對 175,425、〔七〕節省 1.0%／7.3%／11.6% 對 1.0%／7.4%／11.3%）。

**2026-08-04（第一次）修正**：抽群組的權重已改為「群組機率 × 該群組該等級占比」（見下方 `weight()`）。舊版直接用群組機率當權重，那正是被修正掉的錯誤模型；當時的蒙地卡羅之所以「驗證通過」，就是因為模擬與封閉式用了同一個錯誤假設。修正後的權重已用**拒絕取樣**（只照原始機率抽、等級不符就重抽，完全不用這條公式）獨立驗證，見 [釘選策略評估驗證紀錄.md](釘選策略評估驗證紀錄.md) 附錄 B。

以下為可重跑的 node 腳本骨架（需搭配 `遊戲參數整理.txt` 同目錄執行 `node 檔名.js`）：

```js
"use strict";
const fs = require("fs");
// 解析 遊戲參數整理.txt：取得每個群組的機率(groupRate)，
// 以及每個群組內項目的 {group, rate, grade(high/mid/low), rec(是否為 **/# 推薦項)}
const raw = fs.readFileSync("遊戲參數整理.txt", "utf8");
const groupRate = new Map();
for (const l of raw.split(/\r?\n/)) {
  const m = l.match(/^附加能力群組\s*(\d+)\s*\|\s*([\d.]+)%/);
  if (m) groupRate.set(+m[1], +m[2] / 100);
}
const items = []; let cur = null;
for (const l of raw.split(/\r?\n/)) {
  const g = l.match(/^\[附加能力群組\s*(\d+)\]/);
  if (g) { cur = +g[1]; continue; }
  if (cur === null) continue;
  const m = l.match(/^(\*\*|\*|#)?\s*(.+?)\s*\|\s*([\d.]+)%\s*$/);
  if (!m || /^附加能力群組/.test(m[2])) continue;
  const k = m[1] || "";
  items.push({
    group: cur, rate: +m[3] / 100,
    grade: cur === 19 ? "high" : (k === "*" || k === "**") ? "mid" : "low",
    rec: k === "**" || k === "#",
  });
}
const GROUPS = [...groupRate.keys()].filter((g) => g <= 18);
const midBy = new Map(GROUPS.map((g) => [g, items.filter((i) => i.group === g && i.grade === "mid")]));
const lowBy = new Map(GROUPS.map((g) => [g, items.filter((i) => i.group === g && i.grade === "low")]));

// 【2026-08-04 修正】群組權重＝群組機率 × (該群組該等級項目合計 ÷ 該群組全部項目合計)
// 舊版直接用 groupRate.get(x) 當權重，等於假設每個群組吐出該等級的機會相同，
// 會把群組11/12/17/18（項目全為中級）嚴重低估。分母用群組自身合計而非 100：
// 群組3 補齊後 19 個群組合計皆為 100.00%，兩者等價，但保留除法在資料變動時才不會算錯。
// 此權重已用拒絕取樣獨立驗證。
const total = (list) => list.reduce((s, i) => s + i.rate, 0);
const groupItemTotal = new Map(GROUPS.map((g) =>
  [g, total(items.filter((i) => i.group === g))]));
const gradeShare = (grade, g) =>
  total((grade === "mid" ? midBy : lowBy).get(g)) / groupItemTotal.get(g);
const weight = (grade, g) => groupRate.get(g) * gradeShare(grade, g);

function pick(list, weightOf) {
  let total = 0; for (const x of list) total += weightOf(x);
  let r = Math.random() * total;
  for (const x of list) { r -= weightOf(x); if (r <= 0) return x; }
  return list[list.length - 1];
}
// 兩階段抽取：先在未占用群組中依修正後權重抽群組，再於群組內依項目機率抽項目
function drawSlot(grade, used) {
  const by = grade === "mid" ? midBy : lowBy;
  const pool = GROUPS.filter((g) => !used.has(g) && by.get(g).length > 0);
  const g = pick(pool, (x) => weight(grade, x));
  used.add(g);
  return { group: g, rec: pick(by.get(g), (x) => x.rate).rec };
}

const COST = [2000, 3000, 5000];
// 路線 a：釘紫+藍，最後拼白
const policyA = (board) => ({ P: board.P, B: board.B.rec, W: [] });

function runOnce(policy) {
  let pin = { P: false, B: false, W: [] };
  let board = { P: false, B: { rec: false, group: null }, W: [null, null, null] };
  let cost = 0;
  for (let roll = 0; roll < 500000; roll += 1) {
    cost += COST[(pin.P ? 1 : 0) + (pin.B ? 1 : 0) + pin.W.length];
    const used = new Set([19]); // 紫字固定占用群組19
    if (pin.B) used.add(board.B.group);
    for (const i of pin.W) used.add(board.W[i].group);
    const next = { P: pin.P || Math.random() < 1 / 9, B: pin.B ? board.B : drawSlot("mid", used), W: [null, null, null] };
    for (let i = 0; i < 3; i += 1) next.W[i] = pin.W.includes(i) ? board.W[i] : null;
    for (let i = 0; i < 3; i += 1) if (!next.W[i]) next.W[i] = drawSlot("low", used);
    board = next;
    if (board.P && board.B.rec && board.W.filter((x) => x.rec).length >= 2) return cost;
    pin = policyA(board); // 換成其他路線的釘選函式即可比較
  }
  return cost;
}

const N = 150000;
const costs = Array.from({ length: N }, runOnce);
const mean = costs.reduce((a, b) => a + b, 0) / N;
console.log("期望總點數：", Math.round(mean).toLocaleString("en-US"));
```

三條路線與優化版的釘選函式（把 `policyA` 換成對應版本即可重算本文任何一個數字）：

```js
// b) 釘藍→加釘白，最後拼紫+白
const policyB = (b) => b.B.rec
  ? { P: false, B: true, W: b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i).slice(0, 1) }
  : { P: false, B: false, W: [] };

// c) 先釘2白，最後拼紫+藍
const policyC = (b) => ({ P: false, B: false, W: b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i).slice(0, 2) });

// a★) 優化版【2026-08-16 修正】：沒中紫字不釘；**藍字到手就釘，不挑群組**；
// 只有在釘白字時才避開群組16（沒有非群組16 的 # 才退而求其次）。
// 舊版還多一條「藍字避開群組16」，在 22 項設定下會多花 8.0%，已移除。
const policyAStar = (b) => {
  if (!b.P) return { P: false, B: false, W: [] };
  if (b.B.rec) return { P: true, B: true, W: [] };
  const cands = b.W.map((x, i) => [x, i]).filter(([x]) => x.rec);
  const non16 = cands.filter(([x]) => x.group !== 16);
  return { P: true, B: false, W: (non16.length ? non16 : cands).slice(0, 1).map(([, i]) => i) };
};

// 〔七〕的策略族：avoidBlue＝藍字不釘該群組，avoidWhite＝釘白字時優先避開該群組
const makeAvoid = (group, avoidBlue, avoidWhite) => (b) => {
  if (!b.P) return { P: false, B: false, W: [] };
  if (b.B.rec && !(avoidBlue && b.B.group === group)) return { P: true, B: true, W: [] };
  const cands = b.W.map((x, i) => [x, i]).filter(([x]) => x.rec);
  const good = avoidWhite ? cands.filter(([x]) => x.group !== group) : cands;
  return { P: true, B: false, W: (good.length ? good : cands).slice(0, 1).map(([, i]) => i) };
};
```

## 附錄 D：〔八〕的條件式模擬方法

〔八〕比較的不是整條路線，而是**特定盤面下的第一手**，所以做法與附錄C 有兩點不同：

**1. 起始盤面用拒絕取樣抽出。** 直接以完整引擎反覆生成盤面，只保留「藍字 `**` 且 3 白中恰好 2 個 `#`」的樣本（紫字必為 miss，否則已完工）。這樣得到的群組分布才是該條件下的**真實條件分布**——不能用「隨便指定兩個推薦白字」代替，因為群組16 在白字池仍佔 16.8% 的火力（27 項時是 31.2%），落點分布本身就是結論的一部分。

```js
function sampleStart() {
  for (;;) {
    const used = new Set([19]);
    const B = drawSlot("mid", used);
    if (!B.rec) continue;                       // 藍字必須是 **
    const W = [drawSlot("low", used), drawSlot("low", used), drawSlot("low", used)];
    if (W.filter((x) => x.rec).length !== 2) continue;  // 恰好 2 個 #
    return { P: false, B, W };                  // 紫字必為 miss
  }
}
```

**2. 「強制第一手 + 相同續盤策略 + 配對取樣」。** 期望花費那張表的每一列，都是同一批 `sampleStart()` 盤面（共用亂數起點，配對法降低變異），只在**第一轉**強制指定釘法，第二轉起一律交給 `policyAStar`。這樣量到的才是「這一個決定」的代價，不會把後續策略差異算進去。配對後直接對每個樣本取差值再求平均，標準誤從 ±217 收斂到 ±308（差值的 95% CI ±604），足以分辨 1% 等級的差距。

```js
function runFrom(start, firstPin) {
  let board = start, pin = firstPin, cost = 0;
  for (;;) {
    cost += COST[(pin.P ? 1 : 0) + (pin.B ? 1 : 0) + pin.W.length];
    board = rollOnce(board, pin);              // rollOnce 同附錄C 的抽取邏輯
    if (board.P && board.B.rec && board.W.filter((x) => x.rec).length >= 2) return cost;
    pin = policyAStar(board);                  // 第二轉起一律用同一套續盤策略
  }
}

const recIdx = (b) => b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i);
const nonG16First = (b) => {                   // 釘白字時優先挑非群組16
  const c = recIdx(b); const n = c.filter((i) => b.W[i].group !== 16);
  return (n.length ? n : c).slice(0, 1);
};
const FIRST_MOVES = {
  "釘 藍+1白": (s) => ({ P: false, B: true,  W: nonG16First(s) }),
  "釘 2白":    (s) => ({ P: false, B: false, W: recIdx(s).slice(0, 2) }),
  "只釘 藍":   (s) => ({ P: false, B: true,  W: [] }),
  "整盤空洗":  (s) => ({ P: false, B: false, W: [] }),
};

const N = 250000;
const acc = Object.fromEntries(Object.keys(FIRST_MOVES).map((k) => [k, []]));
for (let i = 0; i < N; i += 1) {
  const s = sampleStart();                     // 同一個 s 餵給所有第一手（配對）
  for (const [k, mk] of Object.entries(FIRST_MOVES)) acc[k].push(runFrom(s, mk(s)));
}
```

單轉完工率那張表用同一批盤面、各釘法各跑**一轉**即可（600,000 次），再依 `s.B.group === 16` 拆成兩個分支交叉統計。
