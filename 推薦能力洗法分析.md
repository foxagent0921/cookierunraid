# 推薦能力洗法分析：釘紫+藍 vs 釘藍+白 vs 釘白+白

打怪賽雕刻召喚石只能釘選 2 格、其餘 3 格必須重骰。本文比較三種釘法在「**任意**推薦能力」這個目標下的期望花費（點數），資料來源為 `遊戲參數整理.txt`（14 張遊戲截圖整理），機率標記依 `script.js` 的 `RECOMMENDED_ITEMS_BY_GRADE`。

**結論：方案 a 大幅勝出，且可再優化 6.4%。**

> ⚠️ **2026-08-06：推薦白字從 24 項增為 27 項後全面重算**。新增的是群組5 的「出現技能能量果凍」「紅色技能能量獲得量+7-9%」與群組16 的「閃避大師餅乾傷害量增加20%」。**三條路線的排名與所有質性結論都不變**，但這次的變動方向和上一次不同：
>
> - 白字每格 14.93% → **16.14%**，路線 a 144,681 → **130,748**、a★ 132,617 → **122,323**（都變便宜）。
> - **但路線 c 反而變貴了**（483,808 → 495,845）。新增的 3 項有 2 項落在群組5、1 項落在群組16，推薦白字**更集中**在會壓垮藍字的群組1/5/16（火力合計 62.3% → **65.3%**），路線 c 的終局單轉因此從 1.48% 掉到 **1.02%**。c 與 a 的差距從 3.3 倍拉大到 **3.8 倍**。
> - 〔五〕多了一項質變：**釘住群組5 的 `**` 藍字不再無感**（白字每格 −0.10pt → **−1.14pt**），因為群組5 現在有 11 個 `#`。
> - 〔七〕群組16 的優化幅度再縮到省 **6.4%**，且「只避白字」與「只避藍字」的效益已幾乎相等（2.2% 對 2.3%）。
>
> ⚠️ **2026-08-04（第二次）：推薦白字從 23 項增為 24 項後全面重算**（新增群組10 的「體力恢復量增加11-15%」）。當時白字每格 13.72% → 14.93%、路線 a 175,425 → 144,681、〔七〕優化幅度 11.3% → 8.2%。
>
> ⚠️ **2026-08-04（第一次）：已依修正後的群組權重模型全面重算**（修正內容見 [釘選策略評估驗證紀錄.md](釘選策略評估驗證紀錄.md) 第二節）。修正的重點：抽群組時，群組機率必須先乘上「該群組真的吐出該等級的比例」再正規化。
> 本文舊版〔二〕列出的「法A：綜合權重（群組機率×項目機率）直接歸一化」其實接近正確做法，卻被判定為「粗略近似」而採用了法B——**當時的蒙地卡羅之所以支持法B，是因為模擬本身也用了同一個錯誤的群組抽法**，所以驗不出問題。

> 本文與 [釘選策略分析.md](釘選策略分析.md) 比較的是**同一組路線代號 a/b/c，但目標定義不同、結論相反**——那份文件的目標是兩個**指定**的白字數值格（每格約 0.29%～1.87%），本文的目標是「任意」推薦能力（白字每格約 16.1%、藍字約 20.2%）。兩者並不矛盾，差異純粹來自目標機率相差十幾倍。

---

## 一、問題設定

301 階以上召喚石的 5 個附加能力欄位固定為 1 紫 + 1 藍 + 3 白（欄位定義同 [釘選策略分析.md](釘選策略分析.md) 一節）。

**目標**（對應原始提問的三種洗法）：

- **紫字**：必出 1 個（群組19），中目標項機率 = **1/9 ≈ 11.11%**
- **藍字**：必出 1 個，目標 = 9 個標記 `**` 的推薦藍字之一
- **白字**：3 格中要湊到 **2 個**標記 `#` 的推薦白字（27 項之一）

**釘選費用**：洗 0 個釘選 2000 點／轉，釘 1 個 3000 點／轉，釘 2 個 5000 點／轉。

**三種路線**（對應原提問 a/b/c）：

- **a）** 釘紫字＋藍字，重骰 3 白，拼出 2 個推薦白字
- **b）** 釘藍字，先在白字裡拼到 1 個推薦白字並釘住，再重骰「紫字＋剩下 1 個白字」
- **c）** 先在白字裡拼到 2 個推薦白字都釘住，再重骰「紫字＋藍字」

---

## 二、三個關鍵機率

### 紫字：1/9 ≈ 11.11%

群組19（`HIGH_GRADE_GROUP_ID`）固定 9 個項目、各 11.11%，題目已直接給定。

### 藍字：≈ 20.2%

藍字池為群組1–18 的 43 個 `*` 項目，其中 9 個標 `**`：

| 算法 | 藍字命中推薦(`**`)機率 |
| --- | --- |
| ❌ 舊模型：依原始群組機率抽群組，再於群組內藍字項目歸一化 | 18.45% |
| ✅ **修正後：群組權重＝群組機率 × 該群組中級占比（模擬，本文採用）** | **20.18%** |
| 同上，單格封閉式（僅排除群組19） | 20.08% |
| 參考：綜合權重（群組機率×項目機率）直接歸一化、不含群組互斥 | 20.51% |

舊模型的問題是把「**含有**中級項目的群組」一視同仁，但藍字格保證是中級，而群組11/12/17/18 的項目全是中級（抽中必定是中級）、群組5 只有 2.60% 是中級。權重必須按這個比例縮放。

**藍字這一節完全不受 2026-08-06 的資料異動影響**——新增的 3 個標記都是 `#`（白字），`**` 仍是 9 項，所以數值與上一版相同（20.13% 與 20.18% 的差距落在模擬雜訊內）。這也是本次重跑的一個交叉檢查點。

9 個 `**` 藍字分布：群組1（1項）、群組5（4項）、群組16（4項）——**只集中在這三個群組**，這件事後面會反覆用到。

### 白字：每格 ≈ 16.1%

白字池為群組1–18 的低級項目，其中 27 個標 `#`（2026-08-06 新增群組5 的「出現技能能量果凍」「紅色技能能量獲得量+7-9%」與群組16 的「閃避大師餅乾傷害量增加20%」）：

| 算法 | 白字每格命中推薦(`#`)機率 |
| --- | --- |
| 單格封閉式（僅排除群組19） | 16.69% |
| ✅ **模擬（含群組互斥，本文採用）** | **16.14%** |
| 封閉式＋藍字卡位＋3 格互斥（2026-08-11 新增） | 16.10% |
| 參考：24 項時的同口徑數值 | 14.93% |
| 參考：23 項時的同口徑數值 | 13.72% |

> **2026-08-11：封閉式解與模擬對上了。**把「藍字先抽並卡住一個群組」納入封閉式計算後得到 16.10%，落在 600k 模擬的 16.14% 誤差範圍內；原本的 16.69% 則明顯偏高，因為它假設白字面對完整的群組池。`script.js` 的 `getStoneHitRate` 原先走的正是 16.69% 那條路，已同步修正（逐項最大偏差：群組16 白字高估 18.3%、群組13 白字低估 6.0%）。

新增 3 項讓白字每格上升 1.21 個百分點（14.93% → 16.14%），漲幅與上一次（+1.21pt）幾乎相同，但**分布的走向相反**。

27 個 `#` 白字分布與火力（權重 × 群組內占比）：

| 群組 | `#` 項數 | 占推薦白字火力 | 24 項時 |
| --- | --- | --- | --- |
| 16 | 9（+1） | **31.2%** | 30.2% |
| 5 | 11（+2） | **20.2%** | 17.0% |
| 1 | 2 | 13.9% | 15.1% |
| 10 | 2 | 13.9% | 15.1% |
| 15 | 2 | 13.9% | 15.1% |
| 9 | 1 | 6.9% | 7.5% |

關鍵在最後一欄的走向：上一次新增的項目落在群組10（`**` 藍字碰不到的群組），把火力**分散**開來；這一次的 3 項有 2 項在群組5、1 項在群組16，兩者都住著 `**` 藍字，所以火力反而**更集中**回群組1/5/16（合計 62.3% → **65.3%**）。後面〔三〕路線 c 變貴、〔五〕群組5 從無感變成有感、〔七〕優化幅度再縮水，全都是這一件事的後果。

---

## 三、主要結果（全保真模擬，150,000 次／策略）

模擬完整還原「群組不可重複」的兩階段抽取與逐轉計費（方法見附錄C），非公式近似。

| 路線 | 期望總點數 | 標準誤 | 中位數 | 90%分位 | 倍數 |
| --- | --- | --- | --- | --- | --- |
| **a）釘紫+藍，最後拼白** | 130,748 | ±281 | 100,000 | 272,000 | 1.07x |
| b）釘藍→加釘白，最後拼紫+白 | 211,829 | ±512 | 151,000 | 469,000 | 1.73x |
| c）先釘2白，最後拼紫+藍 | 495,845 | ±1,739 | 270,000 | 1,188,000 | 4.05x |
| **a★）a 的優化版（見七）** | **122,323** | ±253 | 95,000 | 251,000 | **1.00x** |

分布右偏明顯（平均 130,748 但中位數只要 100,000）——**抓預算建議看 90% 分位（272,000），不要只看平均值**，否則容易低估需要準備的點數。

**這次新增 3 個 `#` 之後，a 與 b 變便宜，c 卻變貴了**：a 省 9.6%、b 省 5.1%、**c 多花 2.5%**。上一次（23→24 項）三條路線是全面變便宜的，這次不同，原因在〔二〕那張火力表——新增項集中在群組5／16，等於把推薦白字更往「藍字大戶」堆。a 的終局完全靠白字，直接受惠；c 的終局是「紫 × 藍」，白字變好只縮短前置階段，卻同時讓它更容易釘到會壓垮藍字的群組。兩相抵銷後 c 淨變貴，與 a 的差距從 3.3 倍拉大到 **3.8 倍**。

### 為什麼 a 大幅領先

三條路線終局都是 5000 點／轉，勝負全看終局單轉成功率（下表為模擬中實際觀測到的各釘法終局命中率）：

| 終局狀態 | 要同時中什麼 | 單轉成功率 | 24 項時 |
| --- | --- | --- | --- |
| 釘紫+藍（a） | 3 白中 2 個 `#` | **4.90%** | 4.14% |
| 釘紫+白 | `**`藍 + 2白中≥1個`#` | 4.33% | 3.49% |
| 釘藍+白（b） | 紫(1/9) + 2白中≥1個`#` | 2.51% | 2.47% |
| 釘白+白（c） | 紫(1/9) + `**`藍 | **1.02%** | 1.48% |

原則：**該釘住的是最難的兩格**。紫字（11.1%）與藍字（20.2%）都是單格、無備援；白字雖然每格只有 16.1%，但 3 格輪流抽、只要 2 中，自帶備援。a 把兩個最難、無備援的鎖住，讓最便宜、有備援的白字做「3 選 2」；c 反過來，把兩個最難的留到最後一起賭，等於把 1/9 × 20.2% 硬壓在同一轉上。

**注意 c 那一列是往下走的（1.48% → 1.02%）**。它是唯一終局不含白字的路線，所以白字變好抽對它毫無幫助，反而因為推薦白字更集中在群組1/5/16，它釘住的那 2 格更常把 `**` 藍字的來源封死——期望藍字命中率只剩 13.00%（見〔五〕）。其餘三個終局都直接受惠。

---

## 四、追加發現一：紫字這一格，值得釘

**單一釘位隔離測試**（各自單獨釘住，30,000 次）：

| 策略 | 期望點數 | 期望轉數 |
| --- | --- | --- |
| 完全不釘（2000/轉） | 1,898,802 ±11,038 | 949 轉 |
| **只釘紫（3000/轉）** | **333,921 ±1,837** | **114 轉** |
| 只釘藍（3000/轉） | 589,621 ±3,411 | 198 轉 |
| 只釘白（3000/轉，1格） | 857,132 ±6,062 | 287 轉 |

紫字 11.1% 看似三者最低，但它是唯一「單格無備援」中最難的，所以省下的期望轉數最多——釘紫讓單價漲 50%（2000→3000），卻讓期望轉數砍掉 88%。

**直接對照「允許 vs 禁止釘紫」**（各 60,000 次）：

| | 期望點數 |
| --- | --- |
| 禁止釘紫的最佳解（只能藍+白） | 213,106 ±797 |
| 允許釘紫的最佳解（紫+藍） | 128,802 ±441 |

那一個釘位值 **84,300 點（省 39.6%）**。這個比例逐版遞增（30% → 37% → **39.6%**）：白字愈容易湊齊，被紫字卡住的比重就愈高，紫字那一格的相對價值也就愈突出。**「紫字優先」的結論在新資料下又更穩固。**

**小結**：優先序 **紫字 ＞ 藍字 ＞ 白字**。沒中紫字時建議整盤空洗（2000/轉），把釘位留給紫字，不要為了保留藍/白而多付釘選費——這個調整能再省約 1%（在雜訊範圍邊緣，順手做就好，不必勉強）。

---

## 五、追加發現二：白字群組互斥（縮池）效應

**問題**：白字選走特定群組後，藍字的候選池變少、機率上升的可能有沒有算進去？

**答案**：有算進去（模擬引擎本來就是重新歸一化的生成過程），而且效果**方向不對稱**——18 個群組裡，封鎖任一個對藍字命中 `**` 的影響：

| 封鎖的群組 | 藍字命中`**` | 變化 |
| --- | --- | --- |
| 群組6 | 22.40% | **+2.32pt ↑**（最大漲幅） |
| 群組17 / 10 | 21.59% / 21.58% | +1.51 / +1.50pt ↑ |
| 群組3 | 21.27% | +1.19pt ↑ |
| 群組2,4,7,8,9,11,12,13,14,15 | 20.80～20.81% | +0.72～0.73pt ↑ |
| 群組18 | 20.54% | +0.46pt ↑ |
| 群組5 | 19.01% | −1.08pt ↓ |
| 群組1 | 17.23% | −2.86pt ↓ |
| **群組16** | **7.91%** | **−12.18pt ↓**（最大跌幅） |

（基準 20.08%，封閉式）18 個群組裡 **15 個是上升的**，只有 1、5、16 下降——因為 `**` 只住在這三個群組。**這張表完全不受 2026-08-06 的資料異動影響**（新增的都是 `#` 白字），與上一版相同。

**但路線 c 剛好最容易踩到下降的那 3 個**：`#` 白字火力有 **65.3%** 來自群組1/5/16（24 項時是 62.3%，這次變得更集中）。把「釘住的2個 `#` 白字」實際落點依權重算出來：

- 藍字機率**上升的機率只有 17.8%**，下降 82.2%（24 項時是 18.9% / 81.1%）
- 期望 `**` 機率從 20.08% 掉到 **13.00%**（24 項時是 13.31%）

而且「成本 = 1/機率」不是線性的，壞配置的懲罰被放大（Jensen 效應）：

```
期望終局成本 E[1/p] = 505,534 點
若用「期望機率 13.00%」反推 1/E[p] 只有 346,277 點
```

差的 15.9 萬點，就是壞配置在期望值裡被放大的權重。最常落到的四種組合全都含群組16：

| 釘住的白字群組 | 出現機率 | 終局單轉成功率 |
| --- | --- | --- |
| 群組5 + 16 | 13.2% | 0.547% |
| **群組1 + 16** | 10.7% | **0.413%**（最糟） |
| 群組15 + 16 | 10.7% | 0.919% |
| 群組10 + 16 | 9.7% | 0.963% |
| 群組1 + 5 | 7.6% | 1.756% |
| 群組5 + 15 | 7.5% | 2.195% |

**這才是路線 c 真正貴的原因**，比單看平均機率更嚴重。上一次（23→24 項）新增項落在群組10，把落點稀釋得平均一些、c 因此變便宜；**這一次方向相反**——群組5 拿到 2 個新 `#` 後躍居第二大來源，「群組5+16」這個雙重踩雷的組合出現率從 10.2% 升到 13.2%，成了最常見的落點。這就是 c 這次淨變貴的直接原因。

**反方向也檢查了**：釘住 `**` 藍字後，白字池只會下降、不會上升——因為 `**` 只在群組1/5/16，而這三個群組同時是白字大戶：

| 藍字來自 | 白字`#`每格 | 變化 | 24 項時的變化 |
| --- | --- | --- | --- |
| 群組5 | 15.56% | **−1.14pt** | −0.10pt |
| 群組1 | 15.08% | −1.62pt | −1.45pt |
| 群組16 | 13.33% | −3.36pt | −2.36pt |

（基準 16.69%，封閉式）沒有上升分支，因為藍/白的推薦目標在群組16 是完全互斥的（見七）。**這裡出現了一個質變：群組5 從「幾乎無感」變成「明顯有代價」**（−0.10pt → −1.14pt）。群組5 現在有 11 個 `#`、占白字火力 20.2%，釘住它的 `**` 藍字要付的代價已經接近群組1。實務上「藍字避開群組16」這條規則的收益因此下降（見〔七〕）——因為退而求其次選到的群組5 藍字，也不再是免費的了。

**抽取順序的敏感度**（藍字先抽 vs 白字先抽，各 40,000 次）：

| 路線 | 藍先抽 | 白先抽 | 差異 |
| --- | --- | --- | --- |
| a | 129,518 ±541 | 131,222 ±542 | +1.3%（勉強超出雜訊） |
| b | 209,551 ±977 | 210,307 ±973 | +0.4%（雜訊內） |
| c | 493,654 ±3,341 | 553,232 ±3,694 | **+12.1%** |

只有 c 明顯對順序敏感——因為群組16 的藍字權重高，「白字先抽走群組16」對藍字的傷害最大；推薦白字這次更集中到群組5/16 之後，敏感度還從 10.3% 升到 12.1%。路線 a 的 +1.3%（1,704 ±1,500）只是勉強跨出 95% 信賴區間，量級可以忽略。本文採用的順序（藍先抽）對 c 較有利，所以排名結論不受此假設影響：即使用對 c 最有利的假設，它仍是三者中最貴的。

---

## 六、追加發現三：紫字有 1/9，值得釘嗎？

**是的，而且是三格裡最值得的一格。** 已在〔四〕用「禁止釘紫」的顯式對照證明（省 39.6%）。這裡補充直覺：1/9 看似偏高，但它比另外兩個對手更慘的地方在於「單格無備援」——藍字同樣單格無備援但機率更高（20.2%），白字雖然每格才 16.1% 卻有 3 格輪流抽、只要 2 中的容錯。三者放在一起比較，紫字才是最該優先鎖住的。推薦白字增為 27 項後更好湊，瓶頸進一步集中到紫字，這個結論只會更成立。

---

## 七、追加發現四：藍字該不該接受群組16？

群組16 的推薦項目彼此互斥（同一顆石頭不可能同時出現）：

| 稀有度 | 內容（4條技能線 × 對應數值） |
| --- | --- |
| `**`藍字（4項） | 果凍魔法師／閃避大師 餅乾傷害量增加**50%**、2個以上時增加**50%** |
| `#`白字（9項） | 同 4 條技能線的 **30% / 40%** 版本，外加「閃避大師餅乾傷害量增加**20%**」 |

釘住群組16 的 `**` 藍字，會把 9 個 `#` 白字（31.2% 的白字火力）一起排除，白字每格從 16.69% 掉到 **13.33%**。

**120,000 次／策略、附標準誤的對照**：

| 策略 | 期望總點數 | 中位數 |
| --- | --- | --- |
| 有紫才釘，藍白皆不挑群組（基準） | 131,206 ±313 | 101,000 |
| 只有白字避開群組16 | 128,256 ±311 | 97,000 |
| 藍字避開群組16 | 128,159 ±292 | 101,000 |
| **藍、白都避開群組16** | **122,748 ±281** | 96,000 |
| 上一項 + 沒白可釘時仍接受群組16藍 | **122,118 ±282** | 95,000 |

相對基準的節省（95% 信賴區間）：

| 策略 | 節省 | 是否顯著 |
| --- | --- | --- |
| 只有白字避開群組16 | 2,950 ±865（2.2%） | ✓ 顯著 |
| 藍字避開群組16 | 3,047 ±839（2.3%） | ✓ 顯著 |
| 藍、白都避開群組16 | 8,459 ±825（**6.4%**） | ✓ 顯著 |
| 上一項 + 沒白可釘時仍接受16藍 | 9,088 ±827（6.9%） | ✓ 顯著，但與上一列無法區分 |

**這條優化逐版縮水**（11.3% → 8.2% → **6.4%**），但仍然值得做。這次縮水的機制和上次不同：上次是群組10 提供了替代火力，這次則是**退路本身變貴了**——群組5 拿到 2 個新 `#` 之後，釘住群組5 的 `**` 藍字也要付 −1.14pt 的代價（見〔五〕），不再是免費的次佳選擇。

**質變：「藍字避開群組16」不再是主要效益來源。** 用同一支腳本、同一組策略定義在 24 項與 27 項下各跑一次（避免跨版本定義不一致，見附錄C 的重現檢查）：

| 策略 | 24 項 | 27 項 |
| --- | --- | --- |
| 只有白字避開群組16 | 2.3% | 2.2% |
| **藍字避開群組16** | **4.3%** | **2.3%** |
| 藍、白都避開群組16 | 8.5% | 6.4% |

白字側幾乎沒動，**藍字側直接腰斬**（4.3% → 2.3%）。原因就是群組5 這條退路變貴：以前拒絕群組16 的藍字後，退到群組5 幾乎不用付代價（白字每格只掉 0.10pt），現在要掉 1.14pt。兩者合起來 6.4% 仍大於各自相加（2.2% + 2.3%），交互作用的機制不變：只有當你開始拒絕釘群組16 的藍字，你才會頻繁面臨「釘哪個白字」的抉擇，這時避開群組16 的白字才有意義。

**最後一列的方向翻了，但仍無法區分**：「沒白可釘時仍接受群組16藍」這次比「藍、白都避開」**便宜** 630 點（±795），上一版是貴 338 點（±924）。兩版都落在雜訊內，結論不變——實務上兩種做法可視為等價，不必為了這條規則增加操作負擔。

**重要但書**：這條優化只適用於「任意 `**` 藍字都算贏」的目標。如果實際需求是**指定**要群組16 的某條特定藍字（例如「2個閃避大師以上時餅乾傷害量增加50%」），上述分析不適用——必須接受釘住群組16、白字只能從群組1/5/9/10/15 的 18 個 `#` 裡湊。

---

## 八、追加發現五：盤面已有「1 藍 + 2 白」時，該釘哪兩格？

前面比較的是三條**路線**，這一節處理實戰中最常被問到的**單一決策點**。

**情境**：某一轉開出 **紫字沒中、藍字是 `**` 推薦、3 格白字裡恰好 2 個是 `#` 推薦**。只差紫字就完工（紫字若也中就直接結束了）。兩個候選第一手，**都是 5000 點／轉**：

- **釘 藍+1白** — 重骰 紫 + 2 白，完工需要 紫(1/9) **且** 2 白中至少 1 個 `#`
- **釘 2白** — 重骰 紫 + 藍 + 白，完工需要 紫(1/9) **且** 藍字中 `**`

**結論：釘 藍+1白，不要釘 2 白。** 但差距比想像中小，而且有一個分支會幾乎打平。

### 1｜單轉完工率

600,000 次條件式模擬（起始盤面以拒絕取樣抽出，符合上述條件）：

| 盤面分支 | 出現率 | 釘 藍+1白 | 釘 2白 | 倍數 |
| --- | --- | --- | --- | --- |
| 藍字來自群組16 | 63.9% | 2.424% | 2.123% | 1.14x |
| **藍字非群組16** | 36.1% | **2.901%** | 1.333% | **2.18x** |
| 合計 | 100% | **2.596%** | 1.837% | 1.41x |

### 2｜為什麼釘 2 白會輸

同樣是丟掉一格已經到手的推薦項，**補回來的難度差很多**：

| 丟掉的東西 | 能補的格數 | 補回機率 |
| --- | --- | --- |
| 藍字（釘 2白 的代價） | 1 格，沒有備援 | **16.5%** |
| 1 個白字（釘 藍+1白 的代價） | 2 格輪流抽 | **23.4%** |

> **2026-08-11 修正：**原本這兩格寫 20.2% / 29.7%（後者來自 `1 − (1−16.14%)²`），與上一張表的模擬完工率互相矛盾。兩個補回機率應直接由模擬值換算（`2.596% ÷ 1/9 = 23.4%`、`1.837% ÷ 1/9 = 16.5%`）才是同一口徑。錯誤有兩層：一是 `1 − (1−p)²` 是獨立近似，但兩個白字格會互相排除群組並不獨立（差 0.16～0.59pt）；二是 p 用錯了——藍字與 1 白都已釘住時，兩個群組已被卡掉，每格只剩約 **11.3%**，不是一般盤面的 16.1%。**結論不變**（白字備援仍勝出，且倍率相近），只是絕對值下修。

白字每格在這個情境只剩約 11.3%（兩個群組已被釘走），但**天生有備援**（3 格湊 2，還剩 2 格輪流抽，合起來 23.4%）；藍字只有 1 格、沒有備援，只有 16.5%。釘 2 白等於把唯一沒備援的那格丟回池子，正好違反〔三〕「**該釘住的是最難的兩格**」這條原則。推薦白字增為 27 項後，白字的備援又更厚了，這條原則站得更穩。

### 3｜群組16 分支為什麼會拉近到 1.14x

兩邊同時被群組不可重複影響，方向相反：

- **釘 藍+1白 變差**：釘住群組16 的 `**` 藍字，會封掉 9 個 `#` 白字（31.2% 的白字火力），白字每格從 16.69% 掉到 13.33%。
- **釘 2白 變好**：藍字既然佔著群組16，那 2 個白字就**必定不是**群組16；釘走它們不會封鎖群組16，重骰的藍字仍能抽到那 4 個 `**`。

一降一升，倍數從 2.18x 被壓到 1.14x。而 `**` 藍字有 4/9 住在群組16、群組16 又是藍字格權重最高的群組，所以**這個分支佔了 63.9%**——實戰上比另一個分支更常遇到。

### 4｜整段期望花費

上面是單轉效率。但這顆石頭還要洗很久，真正該比的是**從此盤面到完工的期望總花費**。用同一批起始盤面配對、第一手強制指定、之後一律套用〔七〕的 a★ 策略：

| 第一手 | 期望總花費 | 與「釘 2白」的配對差 |
| --- | --- | --- |
| **釘 藍+1白** | **122,516 ±197** | **−1,174 ±547**（95% CI，顯著） |
| 整盤空洗（2000/轉） | 122,530 ±195 | −1,160 ±543 |
| 只釘 藍（3000/轉） | 122,651 ±195 | −1,039 ±543 |
| 釘 2白 | 123,690 ±197 | — |

（配對 250,000 次）排序與單轉一致，但**幅度只有 1%**。原因是瓶頸仍然是紫字的 1/9：紫字沒中時，怎麼釘都得等，第一手的影響很快被後續的 a★ 續盤吸收掉。值得注意的是**「整盤空洗」與「釘 藍+1白」已經完全打平（−14 ±543）**——多付的 3000 點釘選費，剛好被那一轉高出來的完工率抵銷。前三名彼此無法區分，只有「釘 2白」明確落後。

### 5｜實務做法

**本節唯一穩固的結論是「不要釘 2 白」。** 另外三個選項（釘藍+1白、整盤空洗、只釘藍）彼此的差距都落在雜訊內，挑順手的就好：

1. **不要釘 2 白**——這是四個選項裡唯一明確落後的（比釘藍+1白 貴 1,174 ±547）。
2. **釘藍+1白 與整盤空洗等價**（配對差 −14 ±543）。多付的 3000 點釘選費，剛好被那一轉高出來的完工率抵銷。想省事就照〔九〕第 2 條整盤空洗，想保住手上的藍字就釘藍+1白，兩者一樣。
3. **若真要釘，白字挑非群組16 的那個**（理由同〔七〕）；藍字若剛好是群組16 的推薦項，倍數只剩 1.14x，這時整盤空洗更划算，也與〔七〕「群組16 藍字先別急著釘」一致。
4. **別為這個決定糾結**：整段只差約 1%，真正的分水嶺仍然是紫字有沒有中。

> **這一節沒有推翻〔四〕〔六〕的「紫字優先」。** 那條優先序講的是「**手上已經有**這一格想要的內容時，釘住它值多少」；本節的前提是**紫字沒中**，根本不能釘，所以紫字不在候選名單裡。兩條規則是接力關係：**紫中了就釘紫+藍**（終局 4.90%／轉，全場最強）；**紫沒中就照本節處理**。a★ 策略正是這樣實作的——`if (!b.P) return { P:false, B:false, W:[] }`，紫沒中就整盤不釘、把釘位留給紫字。

> **與〔三〕路線表的數字對照**：〔三〕列出「釘白+白」的終局單轉成功率只有 1.02%，本節卻是 1.84%，兩者不衝突。前者是**整條路線邊際化**的結果（逐轉「先到先釘」慢慢累積 2 白，落點嚴重偏向群組16，最糟的「群組1+16」組合就佔 10.7%）；本節條件在「**單轉同時開出 2 個 `#` 白**」，落點平均得多（含群組16 的比例只有 22.7%），所以成功率較高。推薦白字增為 27 項後兩者的落差還拉大了（1.48% vs 1.86% → 1.02% vs 1.84%），因為路線邊際化那一側惡化得更快。

---

## 九、最終建議

1. **走路線 a**：釘紫+藍，重骰白字拼 2 個推薦白字。比 b 快 1.6 倍、比 c 快 3.8 倍。
2. **沒中紫字時整盤空洗**（2000點/轉），不要為了保留藍/白多付釘選費。
3. **藍字若剛好是群組16 的 `**`，先別急著釘**——留給第二釘位去挑白字（省 6.4%）。等白字湊到 1 個推薦項後，若已經沒有非群組16 的白字可釘，接受或不接受群組16 的藍字都可以，兩者差異在雜訊內。
4. **紫字沒中、盤面卻已有 1 藍 + 2 白時：唯一要避開的是「釘 2 白」**（比釘藍+1白 貴 1,174 ±547）。第 2 條的整盤空洗與釘藍+1白 是等價的（配對差 −14 ±543），兩種做法都行。注意這一條**不是**「紫字優先」的例外——紫字沒中就不能釘，本來就不在選項裡。詳見〔八〕。
5. **預算抓 90% 分位（約 251,000～272,000 點）**，不要只用平均值規劃，因為花費分布右偏、中位數遠低於平均值。

按 1–4 執行後，期望總花費約 **122,000 點**（中位數 95,000）。

---

## 附錄 A：`**`/`#` 推薦項清單

**推薦藍字 `**`（9項）：**

| 群組 | 項目 |
| --- | --- |
| 1 | 餅乾造成的傷害量增加41-50% |
| 5 | 黃色/紅色/藍色技能傷害量增加41-50%、出現更多技能能量果凍 |
| 16 | 果凍魔法師餅乾傷害量增加50%、閃避大師餅乾傷害量增加50%、2個果凍魔法師以上時餅乾傷害量增加50%、2個閃避大師以上時餅乾傷害量增加50% |

**推薦白字 `#`（27項，依群組）：** 群組1（2項）、群組5（**11項**）、群組9（1項）、群組10（2項：體力恢復量增加11-15%／16-20%）、群組15（2項）、群組16（**9項**，見七）。2026-08-06 新增的 3 項為群組5 的「出現技能能量果凍」「紅色技能能量獲得量+7-9%」與群組16 的「閃避大師餅乾傷害量增加20%」。完整清單見 `遊戲參數整理.txt` 中標 `#` 的項目，或 `script.js` 的 `RECOMMENDED_ITEMS_BY_GRADE`。

## 附錄 B：資料限制

群組3 的項目機率合計僅 62.12%（截圖未完整揭露），但該群組沒有任何 `#`／`**` 標記項目（2026-08-06 新增的 3 個 `#` 落在群組5 與群組16，不改變這一點），只影響白字池歸一化時的「雜訊比例」。修正後模型以可見項目重新正規化，等於假設沒有未揭露項目——這使群組3 的低級占比算成 97.55%（而非除以 100 得到的 60.60%），白字格抽中群組3 的機率因此略為上升，稀釋掉一點推薦命中率。由於群組3 完全沒有推薦項，它只是「雜訊」，不影響三路線的排名。沿用專案原則，不自行推測補齊未顯示的資料。

## 附錄 C：模擬方法與驗證腳本

**方法**：全保真 Monte Carlo，每一轉都用「先抽群組（不可重複）、再於群組內依項目機率抽項目」的兩階段流程產生 1 紫 + 1 藍 + 3 白，逐轉依已釘選格數計費（2000/3000/5000），直到終局條件（紫中目標 且 藍中`**` 且 白字≥2個`#`）達成為止，取多次模擬的平均總花費。

**2026-08-06 重跑**：本文所有模擬數字已在推薦白字增為 27 項後重跑。依慣例先做重現檢查——把新增的 3 個 `#` 旗標關掉、還原成 24 項設定，看能否重現前一版的數字：

| 項目 | 24 項設定重現值 | 前一版記錄 | 判定 |
| --- | --- | --- | --- |
| 白字每格命中 `#` | 14.90% | 14.93% | ✓ 雜訊內 |
| 路線 a 期望總點數 | 145,673 ±320 | 144,681 ±317 | ⚠ 高 0.7%（約 2σ） |
| 〔七〕基準 | 144,955 | 144,335 | ✓ 雜訊內 |
| 〔七〕藍字避開群組16 | 省 4.3% | 省 4.4% | ✓ |
| 〔七〕藍、白都避開群組16 | 省 8.5% | 省 8.2% | ✓ |
| 〔七〕只有白字避開群組16 | 省 **2.3%** | 省 **1.4%** | ✗ **對不上** |

**兩點要說清楚**：

1. 路線 a 高出 0.7%（約 2σ），量級可忽略，但不是完美重現，可能來自實作細節的微小差異。
2. **「只有白字避開群組16」對不上**（2.3% vs 1.4%）。這一節的策略定義沒有寫進附錄C 的程式碼，本次是照文字敘述重新實作的，最可能的原因是與原版的策略定義有出入（例如藍字非推薦時是否也挑白字）。因此〔七〕那張「24 項 vs 27 項」對照表**一律使用本次的策略定義重跑兩邊**，不直接沿用前一版記錄的 1.4%，以免拿不同定義的數字相減。

腳本本身未改動——`items` 的 `rec` 旗標直接讀文字檔的 `#`／`**` 標記，所以文字檔改標記後重跑即可。

**2026-08-04（第二次）重跑**：推薦白字由 23 項增為 24 項後重跑；重跑前已用 23 項設定驗證腳本能重現前一版的數字（白字每格 13.73% 對 13.72%、路線 a 176,513 對 175,425、〔七〕節省 1.0%／7.3%／11.6% 對 1.0%／7.4%／11.3%）。

**2026-08-04（第一次）修正**：抽群組的權重已改為「群組機率 × 該群組該等級占比」（見下方 `weight()`）。舊版直接用群組機率當權重，那正是被修正掉的錯誤模型；當時的蒙地卡羅之所以「驗證通過」，就是因為模擬與封閉式用了同一個錯誤假設。修正後的權重已用**拒絕取樣**（只照原始機率抽、等級不符就重抽，完全不用這條公式）獨立驗證，見 [釘選策略評估驗證紀錄.md](釘選策略評估驗證紀錄.md) 附錄 B。

以下為可重跑的 node 腳本骨架（需搭配 `遊戲參數整理.txt` 同目錄執行 `node 檔名.js`）：

```js
"use strict";
const fs = require("fs");
// 解析 遊戲參數整理.txt：取得每個群組的機率(groupRate)，
// 以及每個群組內項目的 {group, rate, grade(high/mid/low), rec(是否為 **/# 推薦項)}
const raw = fs.readFileSync("遊戲參數整理.txt", "utf8");
const groupRate = new Map();
for (const l of raw.split(/\r?\n/)) {
  const m = l.match(/^附加能力群組\s*(\d+)\s*\|\s*([\d.]+)%/);
  if (m) groupRate.set(+m[1], +m[2] / 100);
}
const items = []; let cur = null;
for (const l of raw.split(/\r?\n/)) {
  const g = l.match(/^\[附加能力群組\s*(\d+)\]/);
  if (g) { cur = +g[1]; continue; }
  if (cur === null) continue;
  const m = l.match(/^(\*\*|\*|#)?\s*(.+?)\s*\|\s*([\d.]+)%\s*$/);
  if (!m || /^附加能力群組/.test(m[2])) continue;
  const k = m[1] || "";
  items.push({
    group: cur, rate: +m[3] / 100,
    grade: cur === 19 ? "high" : (k === "*" || k === "**") ? "mid" : "low",
    rec: k === "**" || k === "#",
  });
}
const GROUPS = [...groupRate.keys()].filter((g) => g <= 18);
const midBy = new Map(GROUPS.map((g) => [g, items.filter((i) => i.group === g && i.grade === "mid")]));
const lowBy = new Map(GROUPS.map((g) => [g, items.filter((i) => i.group === g && i.grade === "low")]));

// 【2026-08-04 修正】群組權重＝群組機率 × (該群組該等級項目合計 ÷ 該群組全部項目合計)
// 舊版直接用 groupRate.get(x) 當權重，等於假設每個群組吐出該等級的機會相同，
// 會把群組11/12/17/18（項目全為中級）嚴重低估。分母用群組自身合計而非 100，
// 因為群組3 只揭露 62.12%。此權重已用拒絕取樣獨立驗證。
const total = (list) => list.reduce((s, i) => s + i.rate, 0);
const groupItemTotal = new Map(GROUPS.map((g) =>
  [g, total(items.filter((i) => i.group === g))]));
const gradeShare = (grade, g) =>
  total((grade === "mid" ? midBy : lowBy).get(g)) / groupItemTotal.get(g);
const weight = (grade, g) => groupRate.get(g) * gradeShare(grade, g);

function pick(list, weightOf) {
  let total = 0; for (const x of list) total += weightOf(x);
  let r = Math.random() * total;
  for (const x of list) { r -= weightOf(x); if (r <= 0) return x; }
  return list[list.length - 1];
}
// 兩階段抽取：先在未占用群組中依修正後權重抽群組，再於群組內依項目機率抽項目
function drawSlot(grade, used) {
  const by = grade === "mid" ? midBy : lowBy;
  const pool = GROUPS.filter((g) => !used.has(g) && by.get(g).length > 0);
  const g = pick(pool, (x) => weight(grade, x));
  used.add(g);
  return { group: g, rec: pick(by.get(g), (x) => x.rate).rec };
}

const COST = [2000, 3000, 5000];
// 路線 a：釘紫+藍，最後拼白
const policyA = (board) => ({ P: board.P, B: board.B.rec, W: [] });

function runOnce(policy) {
  let pin = { P: false, B: false, W: [] };
  let board = { P: false, B: { rec: false, group: null }, W: [null, null, null] };
  let cost = 0;
  for (let roll = 0; roll < 500000; roll += 1) {
    cost += COST[(pin.P ? 1 : 0) + (pin.B ? 1 : 0) + pin.W.length];
    const used = new Set([19]); // 紫字固定占用群組19
    if (pin.B) used.add(board.B.group);
    for (const i of pin.W) used.add(board.W[i].group);
    const next = { P: pin.P || Math.random() < 1 / 9, B: pin.B ? board.B : drawSlot("mid", used), W: [null, null, null] };
    for (let i = 0; i < 3; i += 1) next.W[i] = pin.W.includes(i) ? board.W[i] : null;
    for (let i = 0; i < 3; i += 1) if (!next.W[i]) next.W[i] = drawSlot("low", used);
    board = next;
    if (board.P && board.B.rec && board.W.filter((x) => x.rec).length >= 2) return cost;
    pin = policyA(board); // 換成其他路線的釘選函式即可比較
  }
  return cost;
}

const N = 150000;
const costs = Array.from({ length: N }, runOnce);
const mean = costs.reduce((a, b) => a + b, 0) / N;
console.log("期望總點數：", Math.round(mean).toLocaleString("en-US"));
```

三條路線與優化版的釘選函式（把 `policyA` 換成對應版本即可重算本文任何一個數字）：

```js
// b) 釘藍→加釘白，最後拼紫+白
const policyB = (b) => b.B.rec
  ? { P: false, B: true, W: b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i).slice(0, 1) }
  : { P: false, B: false, W: [] };

// c) 先釘2白，最後拼紫+藍
const policyC = (b) => ({ P: false, B: false, W: b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i).slice(0, 2) });

// a★) 優化版：沒中紫字不釘；藍字避開群組16；白字沒有非群組16的#時才退而求其次
const policyAStar = (b) => {
  if (!b.P) return { P: false, B: false, W: [] };
  if (b.B.rec && b.B.group !== 16) return { P: true, B: true, W: [] };
  const cands = b.W.map((x, i) => [x, i]).filter(([x]) => x.rec);
  const non16 = cands.filter(([x]) => x.group !== 16);
  return { P: true, B: false, W: (non16.length ? non16 : cands).slice(0, 1).map(([, i]) => i) };
};
```

## 附錄 D：〔八〕的條件式模擬方法

〔八〕比較的不是整條路線，而是**特定盤面下的第一手**，所以做法與附錄C 有兩點不同：

**1. 起始盤面用拒絕取樣抽出。** 直接以完整引擎反覆生成盤面，只保留「藍字 `**` 且 3 白中恰好 2 個 `#`」的樣本（紫字必為 miss，否則已完工）。這樣得到的群組分布才是該條件下的**真實條件分布**——不能用「隨便指定兩個推薦白字」代替，因為群組16 在白字池佔 31.2% 的火力，落點分布本身就是結論的一部分。

```js
function sampleStart() {
  for (;;) {
    const used = new Set([19]);
    const B = drawSlot("mid", used);
    if (!B.rec) continue;                       // 藍字必須是 **
    const W = [drawSlot("low", used), drawSlot("low", used), drawSlot("low", used)];
    if (W.filter((x) => x.rec).length !== 2) continue;  // 恰好 2 個 #
    return { P: false, B, W };                  // 紫字必為 miss
  }
}
```

**2. 「強制第一手 + 相同續盤策略 + 配對取樣」。** 期望花費那張表的每一列，都是同一批 `sampleStart()` 盤面（共用亂數起點，配對法降低變異），只在**第一轉**強制指定釘法，第二轉起一律交給 `policyAStar`。這樣量到的才是「這一個決定」的代價，不會把後續策略差異算進去。配對後直接對每個樣本取差值再求平均，標準誤從 ±217 收斂到 ±308（差值的 95% CI ±604），足以分辨 1% 等級的差距。

```js
function runFrom(start, firstPin) {
  let board = start, pin = firstPin, cost = 0;
  for (;;) {
    cost += COST[(pin.P ? 1 : 0) + (pin.B ? 1 : 0) + pin.W.length];
    board = rollOnce(board, pin);              // rollOnce 同附錄C 的抽取邏輯
    if (board.P && board.B.rec && board.W.filter((x) => x.rec).length >= 2) return cost;
    pin = policyAStar(board);                  // 第二轉起一律用同一套續盤策略
  }
}

const recIdx = (b) => b.W.map((x, i) => [x, i]).filter(([x]) => x.rec).map(([, i]) => i);
const nonG16First = (b) => {                   // 釘白字時優先挑非群組16
  const c = recIdx(b); const n = c.filter((i) => b.W[i].group !== 16);
  return (n.length ? n : c).slice(0, 1);
};
const FIRST_MOVES = {
  "釘 藍+1白": (s) => ({ P: false, B: true,  W: nonG16First(s) }),
  "釘 2白":    (s) => ({ P: false, B: false, W: recIdx(s).slice(0, 2) }),
  "只釘 藍":   (s) => ({ P: false, B: true,  W: [] }),
  "整盤空洗":  (s) => ({ P: false, B: false, W: [] }),
};

const N = 250000;
const acc = Object.fromEntries(Object.keys(FIRST_MOVES).map((k) => [k, []]));
for (let i = 0; i < N; i += 1) {
  const s = sampleStart();                     // 同一個 s 餵給所有第一手（配對）
  for (const [k, mk] of Object.entries(FIRST_MOVES)) acc[k].push(runFrom(s, mk(s)));
}
```

單轉完工率那張表用同一批盤面、各釘法各跑**一轉**即可（600,000 次），再依 `s.B.group === 16` 拆成兩個分支交叉統計。
