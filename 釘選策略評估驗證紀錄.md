# 釘選策略評估：計算規則驗證紀錄

驗證日期：2026-08-04
驗證對象：`index.html`「釘選策略評估」分頁的計算邏輯（`script.js` 的 `calculatePinnedStrategy`、`getWhiteTargetSetRate`、`getConditionalGroupRate`、`getGroupGradeWeight`、`getItemRateInGrade`），以及欄位即時回饋的機率顯示（`updateTargetFeedback`）。
驗證依據：`遊戲參數整理.txt` 記載的召喚石扭蛋條件限制。

**結論：計算規則符合遊戲的各項條件限制。** 封閉式計算與獨立蒙地卡羅模擬一致，smoke test 通過。

> ⚠️ **2026-08-04 模型修正（重要）**：本日修正了群組權重的計算方式，**頁面上所有機率與期望花費都已改變**。本文件已全面改寫為修正後的數字；先前版本記載的數值（例如五格釘紫藍 0.001842%）已作廢。修正內容見第二節。

---

## 一、條件限制逐項核對

| 條件限制（遊戲參數整理.txt） | 實作方式 | 結果 |
| --- | --- | --- |
| 附加能力群組不可重複抽取 | `getWhiteTargetSetRate` 遞迴逐格排除已抽群組；`calculatePinnedStrategy` 以 `usedGroups` 排除已釘欄位的群組；UI 候選清單與 `getStrategyTargets` 也各自擋下重複群組 | ✅ 符合 |
| 抽取順序：群組 → 項目 → 數值 | 每格機率＝「條件群組機率 × 群組內項目機率」兩層相乘；條件群組機率以 `getGroupGradeWeight` 反映該群組真的吐出該等級的比例（見第二節）；同名能力的數值格在資料中已展開為獨立項目 | ✅ 符合 |
| 301階以上必定出現 1 種高級能力（群組19） | 紫字格恆為群組19；未釘選時指定內容命中率固定 1/9（9 項各 11.11%） | ✅ 符合 |
| 31階以上必定出現 1 種中級能力 | 模型固定 1 個藍字格，中級能力只會出現在這一格；群組11/12/17/18（純中級群組）不參與白字抽取、群組19 不參與藍字與白字抽取 | ✅ 符合 |
| 只能釘選 2 格、其餘 3 格重骰 | 釘選按鈕上限 2 格；`strategyPinnedIds.size !== 2` 或釘選欄位未設定內容時直接不計算 | ✅ 符合 |
| 等級限制（紫＝群組19高級、藍＝中級、白＝低級） | 選取時由 `getTargetCandidates` 過濾，計算前由 `getStrategyTargets` 再驗證一次 | ✅ 符合 |

計算細節的正確性確認：

- **白字無位置差異**：`getWhiteTargetSetRate` 的遞迴涵蓋所有抽取順序排列，指定白字出現在任一未釘格皆算命中。
- **留空的白字格仍占用群組**：不限能力的格子抽到的群組會排除後續抽取，符合群組不可重複規則。
- **抽到目標群組但項目未中＝該目標本轉不可能再達成**：群組已被占用，遞迴中此路徑正確歸零。
- **期望花費**＝每轉 5,000 點 ÷ 單轉達成率，與頁面說明一致；僅代表目前兩格已釘後的終局成本。
- **「整顆石頭命中率」的定義**：紫字與藍字各只有 1 格，機率＝條件群組機率 × 群組內項目機率；白字有 3 格且群組不可重複，必須以聯合機率遞迴計算「任一格命中」，只算單格會低估約 3 倍。

## 二、2026-08-04 的群組權重修正

### 錯在哪

修正前，`getConditionalGroupRate` 把「**含有**該等級項目的群組」一律按原始群組機率正規化（中級分母 99.72%、低級分母 99.07%）。這等於假設每個群組吐出該等級能力的機會相同，但實際差距極大：群組11/12/17/18 的項目**全部**是中級，抽中就必定是中級；群組5 只有 2.60% 的項目是中級、群組1 只有 3.03%。

既然藍字格保證是中級，那些純中級群組被選中的機會就必須被放大。修正前的模型會把它們嚴重低估，甚至讓**相對大小整個顛倒**：

| 藍字能力 | 群組機率 | 項目機率 | 相乘（原始表） |
| --- | --- | --- | --- |
| 套用強度150的磁力（群組11） | 0.14% | 100% | 0.1400% |
| 出現更多技能能量果凍（群組5） | 14.13% | 0.20% | 0.02826% |

原始表顯示磁力比果凍常見 4.96 倍，但修正前頁面顯示磁力 0.1404%、果凍 1.0900%，變成果凍常見 7.76 倍。

### 修正方式

群組權重改為 `getGroupGradeWeight()`：

```
權重 = 群組機率 × (該群組內該等級的項目機率合計 ÷ 該群組全部項目機率合計)
```

亦即 Bayes 的 P(群組│等級) ∝ 群組機率 × 該群組吐出該等級的比例。`getConditionalGroupRate` 與 `getWhiteTargetSetRate` 的分母都改用這個權重。

**分母是該群組自己的項目合計，不是 100。** 多數群組的項目合計是 99.92～100.00，差別可忽略；但群組3 的截圖只揭露 62.12%，若寫死除以 100 會把群組3 的低級占比算成 60.60% 而非 97.55%，白字整體分布會明顯偏掉。這一點是先做蒙地卡羅對照時發現的——封閉式與模擬出現約 2～7σ 的系統性偏差，改用群組自身合計後兩者才吻合。此處與 `getItemRateInGrade` 採同一個「以可見項目重新正規化」的假設。

### 影響幅度

**藍字（差異極大）**

| 能力 | 修正前 | 修正後 | 倍率 |
| --- | --- | --- | --- |
| 套用強度150的磁力（群組11） | 0.1404% | 3.4879% | 24.8× |
| 跳躍次數增加3次（群組12） | 0.1404% | 3.4879% | 24.8× |
| 每隔一段時間弱化怪物防禦力（群組17） | 0.1404% | 3.4879% | 24.8× |
| 果凍魔法師餅乾傷害量增加50%（群組16） | 2.3633% | 約 3.45% | 1.46× |
| 餅乾造成的傷害量增加41-50%（群組1） | 4.5828% | 約 3.45% | 0.75× |
| 黃色技能能量獲得量減少13-15%（群組5） | 1.0900% | 0.7046% | 0.65× |

**白字（多數變動小，群組3 例外）**：低級占比多在 96～100%，各群組權重等比縮放後相互抵銷，單格機率大多只差 1% 上下。群組3 因為只揭露 62.12%，可見項目中有 97.55% 是低級，權重反而**上調**：白字格抽中群組3 從 6.01% 變成 9.33%。

## 三、數值驗算（修正後）

1. **獨立封閉式重算**（另行撰寫，不重用網站程式碼）：逐格條件群組機率與白字三格聯合遞迴皆自行推導，結果與 `script.js` 的輸出完全相同。
2. **獨立蒙地卡羅模擬**（拒絕取樣，完全不使用「群組機率 × 等級占比」這條公式，見附錄 B）：

   | 項目 | 封閉式 | 蒙地卡羅 | 次數 |
   | --- | --- | --- | --- |
   | 藍字・黃色技能能量獲得量減少13-15%（群組5） | 0.7046% | 0.7089% | 300 萬 |
   | 藍字・套用強度150的磁力（群組11） | 3.4879% | 3.4812% | 300 萬 |
   | 白字・餅乾造成的傷害量增加1-10%（群組1，三格至少一次） | 3.6829% | 3.6745% | 300 萬 |
   | 五格釘紫藍・三個指定白字聯合 | 0.001873% | 0.001855% | 4,000 萬 |

   四項皆落在 1σ 以內。

3. **smoke test**（`tests/smoke-test.js`）：三格 4.2987%、四格 0.1236%、五格釘紫藍 0.001873%、釘紫＋白 0.000327% 等斷言全數通過。斷言值取自上述獨立重算，不是抄程式輸出。

4. **欄位即時回饋**的四段數字（群組機率／該格抽中此群組／群組內指定能力命中率／整顆石頭命中率）皆與同一組引擎函式一致，例如：

   ```
   群組5｜群組機率 14.13%｜藍字格抽中此群組 9.1601%｜群組內指定能力命中率 7.6923%
   （原始項目機率 0.20% ÷ 群組內中級項目合計 2.60%）｜整顆石頭命中率 0.7046%
   ```

   藍字只有一格，9.1601% × 7.6923% = 0.7046%，整行可自行驗算。

## 四、模型假設與已知限制（非錯誤）

1. **「藍字先抽」為自訂假設**。截圖僅說明群組→項目→數值與群組不可重複，未說明保底中級能力的抽取時點。計算表格標題已註明此假設；若實際順序不同，數字會略有出入。
2. **「恰好 1 個中級」為自訂假設**。遊戲文案是「31階以上一定會出現1種中級附加能力」，未排除出現更多；模型固定為 1 藍 3 白。
3. **群組3 資料不完整**：可見項目機率合計僅 62.12%，`getItemRateInGrade` 與 `getGroupGradeWeight` 都以可見項目重新正規化，等於隱含假設沒有未揭露項目；若目標選在群組3，命中率可能被高估。頁面已有兩處提醒（策略頁「計算方式與假設」卡片、選到群組3能力時的即時回饋），參數速查的規則卡片亦補充說明。
4. **`釘選策略分析.md` 與 `推薦能力洗法分析.md` 皆已依修正後模型重算**（兩份文件開頭都有重算聲明）。`推薦能力洗法分析.md` 另於 2026-08-04 因推薦白字由 23 項增為 24 項（群組10 新增「體力恢復量增加11-15%」）再次全面重跑，重跑前已用 23 項設定驗證腳本能重現前一版數字。該文件〔七〕釘群組16 藍字的取捨結論方向不變，但優化幅度由省 11.3% 縮為省 8.2%。

## 五、待改進事項（不影響計算正確性）

- 釘選某欄位後再清空該欄位文字，釘選狀態會保留；此時空狀態提示顯示「至少需要 3 格目標（已設定 N 格）」，即使 N ≥ 3 也一樣。實際原因是「已釘選的欄位未設定內容」，訊息有誤導性。

  2026-08-04 新增的欄位「清除」鈕會一併解除該欄位的釘選，因此走清除鈕不會進入這個狀態。用鍵盤手動把文字刪光的路徑仍未處理——刻意保留，避免使用者只是想刪字重打就被解除釘選。

## 附錄 A：修正後模型的封閉式重算腳本

純 node 腳本（無外部套件），依「群組 → 項目、群組不可重複、藍字先抽」規則獨立推導。存檔後執行 `node 檔名.js`：

```js
"use strict";
const GROUP_RATES = [
  4.57, 4.57, 9.15, 4.57, 14.13, 13.72, 4.57, 4.57, 4.57, 9.15,
  0.14, 0.14, 2.22, 4.57, 4.57, 14.14, 0.28, 0.09, 0.25,
];
const rate = (g) => GROUP_RATES[g - 1];
const INT_TOTAL = {
  1: 3.03, 2: 3.03, 3: 1.52, 4: 3.03, 5: 13 * 0.20, 6: 3 * 1.01,
  7: 3.03, 8: 3.03, 9: 3.03, 10: 2 * 1.52, 11: 100, 12: 100,
  13: 6.25, 14: 3.03, 15: 3.03, 16: 6 * 0.98, 17: 2 * 50, 18: 100,
};
const LOW_TOTAL = {
  1: 4 * 24.24, 2: 4 * 24.24, 3: 5 * 12.12, 4: 4 * 24.24,
  5: 48 * 1.96 + 3.24, 6: 12 * 8.08, 7: 4 * 24.24, 8: 4 * 24.24,
  9: 4 * 24.24, 10: 8 * 12.12, 13: 5 * 18.75, 14: 4 * 24.24,
  15: 4 * 24.24, 16: 24 * 3.92,
};
// 分母是該群組自己的項目合計（群組3 只揭露 62.12%），不是 100
const GROUP_ITEM_TOTAL = {};
for (const g of Object.keys({ ...INT_TOTAL, ...LOW_TOTAL }).map(Number)) {
  GROUP_ITEM_TOTAL[g] = (INT_TOTAL[g] ?? 0) + (LOW_TOTAL[g] ?? 0);
}
const w = (totals, g) => rate(g) * (totals[g] / GROUP_ITEM_TOTAL[g]);
const LOW_GROUPS = Object.keys(LOW_TOTAL).map(Number);
const INT_GROUPS = Object.keys(INT_TOTAL).map(Number);

function condGroup(totals, pool, g, used = new Set()) {
  const avail = pool.filter((x) => !used.has(x));
  const total = avail.reduce((s, x) => s + w(totals, x), 0);
  return avail.includes(g) && total > 0 ? w(totals, g) / total : 0;
}

// 白字：targets = [{group, itemRate}]，在 slots 格中全部命中（群組不重複）
function whiteSet(targets, used, slots) {
  if (targets.length === 0) return 1;
  if (slots < targets.length || slots <= 0) return 0;
  const avail = LOW_GROUPS.filter((g) => !used.has(g));
  const total = avail.reduce((s, g) => s + w(LOW_TOTAL, g), 0);
  if (total <= 0) return 0;
  let acc = 0;
  for (const g of avail) {
    const p = w(LOW_TOTAL, g) / total;
    const next = new Set(used).add(g);
    const idx = targets.findIndex((t) => t.group === g);
    if (idx >= 0) {
      const rest = targets.filter((_, i) => i !== idx);
      acc += p * targets[idx].itemRate * whiteSet(rest, next, slots - 1);
    } else {
      acc += p * whiteSet(targets, next, slots - 1);
    }
  }
  return acc;
}

const fp = (r) => { const p = r * 100; return p.toFixed(p < 0.01 ? 6 : 4) + "%"; };
const W1 = { group: 1, itemRate: 0.25 };
const W9 = { group: 9, itemRate: 0.25 };
const W10 = { group: 10, itemRate: 0.125 };

console.log("藍字格抽中群組5 :", fp(condGroup(INT_TOTAL, INT_GROUPS, 5)));
console.log("藍字 群組5 指定 :", fp(condGroup(INT_TOTAL, INT_GROUPS, 5) * (0.20 / 2.60)));
console.log("藍字 群組11     :", fp(condGroup(INT_TOTAL, INT_GROUPS, 11)));
console.log("白字 群組1 三格 :", fp(whiteSet([W1], new Set(), 3)));
console.log("三格（釘紫藍）  :", fp(whiteSet([W1], new Set([5]), 3)));
console.log("四格（釘紫藍）  :", fp(whiteSet([W1, W9], new Set([5]), 3)));
console.log("五格（釘紫藍）  :", fp(whiteSet([W1, W9, W10], new Set([5]), 3)));
```

## 附錄 B：拒絕取樣的蒙地卡羅驗證腳本

完全依「先抽群組（照原始群組機率）→ 再抽項目（照原始項目機率）」模擬，等級不符或群組已占用就整次重抽。**這個做法沒有用到「群組機率 × 等級占比」這條公式**，因此可以獨立驗證附錄 A 的權重是否正確：

```js
"use strict";
const GROUP_RATES = [
  4.57, 4.57, 9.15, 4.57, 14.13, 13.72, 4.57, 4.57, 4.57, 9.15,
  0.14, 0.14, 2.22, 4.57, 4.57, 14.14, 0.28, 0.09, 0.25,
];
// 每個群組的等級組成（原始項目機率合計）：[中級, 低級, 高級]
const COMP = {
  1: [3.03, 96.96, 0], 2: [3.03, 96.96, 0], 3: [1.52, 60.60, 0], 4: [3.03, 96.96, 0],
  5: [2.60, 97.32, 0], 6: [3.03, 96.96, 0], 7: [3.03, 96.96, 0], 8: [3.03, 96.96, 0],
  9: [3.03, 96.96, 0], 10: [3.04, 96.96, 0], 11: [100, 0, 0], 12: [100, 0, 0],
  13: [6.25, 93.75, 0], 14: [3.03, 96.96, 0], 15: [3.03, 96.96, 0], 16: [5.88, 94.08, 0],
  17: [100, 0, 0], 18: [100, 0, 0], 19: [0, 0, 99.99],
};
const GROUP_TOTAL = GROUP_RATES.reduce((a, b) => a + b, 0);
const all = (g) => COMP[g][0] + COMP[g][1] + COMP[g][2];

function pickGroup() {
  let r = Math.random() * GROUP_TOTAL;
  for (let g = 1; g <= 19; g += 1) {
    r -= GROUP_RATES[g - 1];
    if (r <= 0) return g;
  }
  return 19;
}

// gradeIdx：0＝中級、1＝低級。回傳 { group, offset }，offset 為該等級區間內的位置
function drawSlot(gradeIdx, used) {
  for (;;) {
    const g = pickGroup();
    if (used.has(g)) continue;
    const u = Math.random() * all(g);
    const [int, low] = COMP[g];
    const start = gradeIdx === 0 ? 0 : int;
    const span = gradeIdx === 0 ? int : low;
    if (span <= 0) continue;
    if (u < start || u >= start + span) continue;
    return { group: g, offset: u - start };
  }
}

const pct = (h, n) => ((h / n) * 100).toFixed(4) + "%";
const pct6 = (h, n) => ((h / n) * 100).toFixed(6) + "%";

let n = 3_000_000, hit = 0, hit11 = 0;
for (let i = 0; i < n; i += 1) {
  const s = drawSlot(0, new Set());
  if (s.group === 5 && s.offset < 0.20) hit += 1;
  if (s.group === 11) hit11 += 1;
}
console.log("藍字 群組5 指定能力 :", pct(hit, n), " 封閉式 0.7046%");
console.log("藍字 群組11（唯一）  :", pct(hit11, n), " 封閉式 3.4879%");

n = 3_000_000;
let hitW = 0;
for (let i = 0; i < n; i += 1) {
  const used = new Set();
  let ok = false;
  for (let k = 0; k < 3; k += 1) {
    const s = drawSlot(1, used);
    used.add(s.group);
    if (s.group === 1 && s.offset < 24.24) ok = true;
  }
  if (ok) hitW += 1;
}
console.log("白字 群組1 三格至少一次:", pct(hitW, n), " 封閉式 3.6829%");

n = 40_000_000;
let hitJoint = 0;
for (let i = 0; i < n; i += 1) {
  const used = new Set([5]);
  let g1 = false, g9 = false, g10 = false;
  for (let k = 0; k < 3; k += 1) {
    const s = drawSlot(1, used);
    used.add(s.group);
    if (s.group === 1 && s.offset < 24.24) g1 = true;
    else if (s.group === 9 && s.offset < 24.24) g9 = true;
    else if (s.group === 10 && s.offset < 12.12) g10 = true;
  }
  if (g1 && g9 && g10) hitJoint += 1;
}
console.log("五格釘紫藍 聯合      :", pct6(hitJoint, n), " 封閉式 0.001873%");
```
